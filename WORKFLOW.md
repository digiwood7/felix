# PET Time — 개발 워크플로우

| | |
|---|---|
| **문서 버전** | v1.1 |
| **작성일** | 2026-08-08 (v1.1 개정 2026-08-10) |
| **근거 문서** | [PRD.md](./PRD.md) v1.1 |
| **개발 방식** | Claude Code 기반 (1인 개발) |

---

## 0. 이 문서의 사용법

PRD가 **무엇을 만들지**를 정의한다면, 이 문서는 **어떤 순서로 어떻게 만들지**를 정의한다.

- 작업 단위는 **Claude Code 세션 1회 = 작업 1개 = 커밋 1개**로 맞춘다.
- 각 작업에는 **DoD(완료 기준)** 가 있다. 코드가 돌아가는 것은 완료 기준이 아니다.
- 각 작업에 **Claude Code 프롬프트 예시**를 붙여 두었다. 그대로 붙여넣고 시작하면 된다.
- 순서를 바꾸지 않는다. 특히 T3(계산 엔진)을 건너뛰고 화면부터 만들지 않는다.

---

## 1. 전체 구조 — 3트랙 병렬

개발만 하면 실패한다. 세 트랙이 동시에 굴러가야 Week 8에 승인을 받는다.

```
        W0    W1    W2    W3    W4    W5    W6    W7    W8
        │     │     │     │     │     │     │     │     │
트랙A   ■■■■■■│     │     │     ■■■■■■■■■■■■■■■■■■│
측정    tally(before)                tally(after)  │
        │     │     │     │     │     │     │     │     │
트랙B   │     ■■■■■■■■■■■■■■■■■■│     │     │     │     │
개발          T0~T7  T8~T10 T11~T13  운영·수정      │
        │     │     │     ▲     │     │     │     │     │
        │     │     │  스티커 배포                  │     │
        │     │     │     │     │     │     │     │     │
트랙C   │     │     │     ●─────────────────────── ●
승인                    1차 보고               QI 발표
                     (프로토타입+                  ↓
                      before 데이터)         안내지 QR 삽입
```

**세 트랙의 의존 관계**

| 의존 | 내용 |
|---|---|
| 트랙A → 트랙B | W0 tally의 "기타 문의 원문"이 문답 5개를 확정한다 (**T8**의 선행 조건) |
| 트랙B → 트랙C | W3 프로토타입이 없으면 1차 보고를 못 한다 |
| 트랙A → 트랙C | before 데이터가 없으면 Week 8에 아무것도 증명할 수 없다 |
| 트랙B → 트랙B | W1 게이트의 직원 피드백이 요약카드 항목과 `triage` level을 확정한다 (T8의 선행 조건) |

**가장 흔한 실패 패턴:** 트랙B만 신나게 돌리다가 Week 8에 "만들긴 했는데 효과를 증명할 수 없는" 상태가 되는 것. 트랙A는 코딩보다 재미없지만 승인의 90%를 결정한다.

---

## 2. 트랙 A — 측정 (W0~W1 수집, W4~W7 재수집)

개발 지식이 필요 없는 트랙. **개발보다 먼저 시작한다.**

> 트랙 제목이 v1.0에서는 "W0, W4~W7"이었으나, before 수집은 **2주(W0~W1)** 다. W0 한 주로 오해하면 표본이 절반이 된다.

### A1. W0 착수 (개발 시작 전)

- [ ] PRD §16 tally 시트 A4 인쇄 (2주치 × 3명 = 30장 여유 있게)
  - **[B]의 "카드" O/X 칸이 들어간 v1.1 시트인지 확인.** 이 칸이 없으면 W8에 Primary KPI를 계산할 수 없다
- [ ] 동료 3명에게 협조 요청
  - 화법: "새 서비스 만들려고요" ✕ → **"우리가 하루에 같은 설명을 몇 번 하는지 세어보려고요"** ○
- [ ] 시작 전 5분 정렬 미팅 — 항목 해석 맞추기
  - 특히 "물어본 것만 표시, 내가 먼저 설명한 건 제외" 규칙
  - **[B] 시간 측정은 그날 창구 담당자 1명만** 기록한다는 점 (3명이 각자 재면 중복)
- [ ] 2주간(W0~W1) 매일 수집, 주 1회 취합

### A2. before 수집 종료 시 산출물 (W1 말)

| 산출물 | 용도 |
|---|---|
| 문의 유형별 주당 건수 | 관리자 보고 슬라이드 1번 |
| 접수 문답 시간 분포 (약 50건 = 5건 × 10영업일) | **Primary KPI의 before 값** |
| 9번 기타 원문 목록 | **T8 문답 확정의 유일한 근거** |

### A3. W4~W7 after 측정

- 동일 시트, 동일 관찰자, **동일 요일 기준으로 비교**
- 배포 후 첫 주(W4)는 워밍업으로 보고 분석에서 제외 가능 — 단, 제외했다면 발표에 명시한다
- **[B]의 "카드" 칸을 반드시 채운다.** Primary KPI는 카드 O인 환자만으로 계산한다 (PRD §4.1)

### A4. 측정 원칙

1. [B] 시간 측정은 **하루 5명, 첫 환자부터 순서대로.** 환자를 고르면 데이터가 죽는다.
2. **[B]는 그날 창구 담당자 1명이 5건.** 관찰자 3명이 각자 5건씩 재는 것이 아니다.
3. 9번 기타는 **환자 말 그대로.** 요약하면 T8의 재료가 사라진다.
4. **after 측정에서 카드 O/X를 빠뜨리지 않는다.** 시간만 적힌 레코드는 Primary KPI 계산에서 버려진다.
5. 데이터가 나쁘게 나와도 그대로 쓴다. "유의한 변화 없음"이 발표 신뢰도를 오히려 높인다.

---

## 3. 트랙 B — 개발 (W1~W3)

### 3.1 디렉토리 구조

```
PET Time/
├── PRD.md
├── WORKFLOW.md
├── CLAUDE.md              ← T0에서 생성. 매 세션 자동 로드되는 가드레일
├── app/
│   ├── page.tsx           # S1 입력
│   ├── [exam]/
│   │   ├── page.tsx       # S2 타임라인
│   │   ├── check/page.tsx # S3 문답
│   │   └── card/page.tsx  # S4 요약카드
│   └── api/log/route.ts   # F4 익명 로그
├── lib/
│   ├── rules/
│   │   ├── f18-fdg-pet.json   ← 룰셋. 이 파일이 제품의 심장
│   │   └── types.ts       # 룰셋 타입 (검사 확장 대비 일반화)
│   ├── schedule.ts        # 시각 계산 엔진 (KST 고정)
│   ├── schedule.test.ts   # 경계값 테스트 (R5 대응)
│   ├── triage.ts          # 문답 → 배지(ok/tell/call). 룰셋 triage 해석
│   ├── encode.ts          # URL 상태 인코딩
│   └── ics.ts             # 캘린더 파일 생성
├── components/
│   ├── Timeline.tsx
│   ├── SummaryCard.tsx
│   ├── Badge.tsx
│   └── Disclaimer.tsx     # §11 필수 문구. 모든 화면에 고정
└── public/qr/             # QR 이미지 · 스티커 시안
```

### 3.2 작업 목록

각 작업 = 세션 1회 = 커밋 1개.

> **v1.1에서 작업 번호가 재배열되었다.** 요약카드(T7)를 W1 안으로 옮겼기 때문이다.
> PRD §8 F2가 "요약카드 항목은 W1 종료 시점에 직원 3명의 피드백으로 확정"이라고 규정했는데,
> v1.0의 배치(카드 = W2)로는 W1 시연 때 보여줄 카드가 없었다. **R4 대응이 실행 계획에서 빠져 있었다.**
>
> | 주차 | 작업 |
> |---|---|
> | **W1** | T0 → T7 → **직원 시연 게이트** |
> | **W2** | T8 → T10 → **40초 완주 게이트** |
> | **W3** | T11 → T13 → **대기실 관찰 게이트** |

---

#### T0. 가드레일 세팅

**목표** — Claude Code가 매 세션 PRD의 타협 불가 원칙을 지키게 만든다.

**작업**
- `CLAUDE.md` 생성 (부록 A 내용 그대로)
- 이 파일이 없으면 세션이 길어질수록 "검사 가능합니다" 같은 금지 문구가 슬금슬금 들어온다

**DoD** — `CLAUDE.md`에 금지 표현 목록과 안전 불변조건이 명시되어 있다

**커밋** — `chore: CLAUDE.md 가드레일 추가`

---

#### T1. 프로젝트 스캐폴딩

**목표** — 빈 화면이 Vercel에 뜬다.

**프롬프트**
```
Next.js(App Router) + TypeScript + Tailwind로 프로젝트를 초기화해줘.
- 한국어 기본, lang="ko"
- 기본 폰트 크기 17px (PRD §13 비기능 요구사항)
- 웹폰트를 넣지 마. 시스템 폰트만 쓴다 (PRD §13)
- 라우트만 빈 껍데기로 만들어줘: / , /pet , /pet/check , /pet/card
- DB나 인증은 넣지 마. v1은 서버 상태를 갖지 않는다.

추가로 두 가지를 같이 세팅해줘:
1. vitest 설치 + npm test 스크립트 (T3에서 테스트를 먼저 쓴다)
2. kill switch — 환경변수 NEXT_PUBLIC_SERVICE_STATUS 가 "maintenance" 이면
   모든 라우트가 아래 화면만 표시하도록 layout에서 처리 (PRD §18)

     지금은 안내를 제공하지 않습니다.
     검사 준비사항은 접수처 또는 1599-3114로 문의해 주세요.
```

**DoD**
- [ ] Vercel 배포 URL에서 4개 라우트가 모두 열린다
- [ ] `npm test` 가 (테스트 0개 상태로) 정상 종료한다
- [ ] `NEXT_PUBLIC_SERVICE_STATUS=maintenance` 로 바꾸면 4개 라우트 전부 점검 화면이 뜬다

> **kill switch를 T1에 넣는 이유:** PRD §18 R5의 대응이 "즉시 배포 중단"인데, 스티커가 벽에 붙은 뒤에는 중단할 수단이 없다. 나중에 넣으려면 계산 로직보다 먼저 평가되도록 구조를 되돌려야 하므로 처음부터 넣는다. 구현 10분.

**커밋** — `feat: Next.js 프로젝트 스캐폴딩`

---

#### T2. 룰셋 JSON + 타입

**목표** — PRD §9.1 룰셋을 파일로 만들고 타입을 붙인다.

**프롬프트**
```
PRD.md §9.1의 룰셋 JSON을 lib/rules/f18-fdg-pet.json 으로 만들고,
lib/rules/types.ts 에 TypeScript 타입을 정의해줘.

중요:
- 검사 종류를 추가할 때 JSON만 추가하면 되도록 타입을 일반화할 것
- levels 값은 판정이 아니라 행동 지시 문구다. 절대 "검사 가능/불가"로 바꾸지 마
- triage 블록의 when 값은 유니온 타입으로 정의할 것 (PRD §9.4)
- triage 는 optional. 없어도 flags 만으로 배지가 판정되어야 한다
- early_morning_shift 는 v1.1에서 제거된 필드다. 넣지 마
```

**DoD**
- [ ] 타입 체크 통과
- [ ] JSON이 PRD §9.1과 한 글자도 다르지 않다
- [ ] `early_morning_shift` 가 JSON·타입 어디에도 없다

**커밋** — `feat: FDG PET 룰셋 JSON 및 타입 정의`

---

#### T3. 시각 계산 엔진 + 테스트 ★ 최우선

**목표** — R5(치명적 리스크)를 잠근다. **이 작업은 테스트를 먼저 쓴다.**

**프롬프트**
```
lib/schedule.ts 에 시각 계산 엔진을 만들어줘.
단, 구현보다 테스트를 먼저 작성해줘 (lib/schedule.test.ts).

입력: 룰셋 + 예약 일시 + 조건 응답(당뇨 여부 등)
출력: 날짜별로 그룹핑된 타임라인 항목 배열

절대 규칙 (PRD §9.2):
1. 모든 시각은 내림(floor) 처리. 반올림 금지.
   - 금식 시작 · 복약 마지노선 → floor_hour
   - 도착 시각 → floor_10min
2. 새벽 보정은 하지 않는다. 계산값을 그대로 쓴다.
   보조 문구가 필요하면 룰셋의 fasting.note 를 읽어 붙인다.
3. 모든 계산은 KST(Asia/Seoul) 고정이다.
   new Date() / getHours() 등 기기 로컬 타임존을 따르는 API를
   계산 경로에 쓰지 마. 룰셋 timezone 필드를 유일한 기준으로 삼는다.

반드시 테스트할 경계값 (기대값을 그대로 단언할 것):
1. 08:25 예약 → 금식 8/6 02:00, 당뇨 8/6 04:00, 도착 8/6 08:00
2. 10:50 예약 → 도착 10:30   ← floor_10min. 시 단위로 내리면 10:00(50분 전)이 되어 틀림
3. 12:00 예약 → 금식 06:00   ← 당일. 새벽 보정이 없으므로 그대로 06:00
4. 06:00 예약 → 금식 00:00   ← 당일 자정. 전날로 넘기지 않는다
5. 05:00 예약 → 금식 전날 23:00
6. 00:30 예약 → 금식 전날 18:00, 당뇨 전날 20:00, 도착 00:10
7. 운동 제한 → 항상 "예약 전날 종일" (restrictions.from: prev_day_start)
8. 월말·연말 넘김 → 1/1 00:30 예약이면 금식 12/31 18:00
9. 윤년 → 2028-03-01 02:00 예약이면 금식 2028-02-29 20:00
10. 기기 타임존이 UTC / America/New_York 여도 결과가 동일할 것
```

**DoD**
- [ ] 위 경계값 10종 테스트가 모두 통과
- [ ] `npm test` 가 CI 없이도 로컬에서 돈다
- [ ] **손으로 계산한 값과 3건 이상 대조 확인**
- [ ] `TZ=UTC npm test` 와 `TZ=America/New_York npm test` 도 통과

> 이 작업이 이 프로젝트에서 유일하게 "틀리면 끝나는" 지점이다. 시간이 부족하면 다른 걸 줄이고 여기에 쓴다.

**v1.1에서 바뀐 것**

| 항목 | v1.0 | v1.1 |
|---|---|---|
| 새벽 보정 | 테스트 케이스에 포함 | **규칙 자체가 제거됨** (PRD §9.2) |
| `00:30 예약` 기대값 | "금식이 전전날로 넘어감" | **전날 18:00.** 전전날이 아니다 — 06:00 이전 예약이라도 6시간 전은 전날 저녁이다 |
| 도착 시각 내림 | `floor` (단위 불명) | `floor_10min` |
| 타임존 | "서머타임 없음 확인" | **기기 타임존 무관 검증.** 한국에 서머타임이 없다는 것보다, 기기 타임존이 KST가 아닐 때가 실제 위험이다 |

**커밋** — `feat: 시각 계산 엔진 + 경계값 테스트`

---

#### T4. 타임라인 화면 (S2)

**목표** — 계산 결과를 사람이 읽는 형태로 보여준다.

**프롬프트**
```
components/Timeline.tsx 와 app/pet/page.tsx 를 만들어줘.
PRD §8 F1의 출력 예시를 그대로 재현할 것.

- 날짜별 그룹 + 요일 표기
- 시각은 20px 이상, 지시문은 17px 이상
- 각 항목은 지시문 1줄 + 보조설명 최대 1줄. 3줄 넘기지 말 것
- 금식 항목의 보조설명은 룰셋 fasting.note 를 읽어서 출력할 것.
  화면에 문구를 하드코딩하지 마
- 금지 음료는 ✕ 아이콘과 함께 나열
- 하단에 components/Disclaimer.tsx 고정 (PRD §11 필수 문구)
```

**DoD**
- [ ] 실제 폰(본인 기기)에서 스크롤 없이 하루치가 읽힌다
- [ ] 금식 시각이 계산값 그대로 표시된다 (08:25 예약 → `02:00`)
- [ ] 보조 문구가 룰셋에서 읽혀 나온다 (JSON의 `note`를 바꾸면 화면이 바뀐다)

**커밋** — `feat: 타임라인 화면 (S2)`

---

#### T5. 입력 화면 (S1) + URL 파라미터

**목표** — QR로 들어온 사람이 10초 안에 타임라인에 도달한다.

**프롬프트**
```
app/page.tsx 에 날짜·시각 입력 화면을 만들어줘.

- URL에 ?t=202608060825 가 있으면 자동 채움 후 즉시 S2로 이동
- 없으면 입력 UI 표시
- 시각은 드롭다운이 아니라 큰 버튼 그리드 (병원 예약은 대부분 정시/30분 단위)
- 개인정보 미입력 안내 문구를 입력 필드 바로 아래 배치

절대 넣지 말 것: 이름, 등록번호, 연락처, 생년월일 입력 필드
```

**DoD** — `?t=` 있을 때와 없을 때 모두 정상 동작

**커밋** — `feat: 입력 화면 및 URL 파라미터 자동 채움 (S1)`

---

#### T6. 문답 플로우 (S3) — 잠정 문항

**목표** — 환자가 40초 안에 자기 상태를 구조화한다.

> **선행 조건이 v1.1에서 삭제되었다.** v1.0은 "W0 tally 분석 완료"를 선행 조건으로 걸었으나, 그러면 요약카드가 W2로 밀려 W1 직원 시연에 카드가 없게 된다. **PRD §8 F2 가안대로 잠정 구현하고, tally 결과와 직원 피드백은 T8에서 함께 반영한다.**

**프롬프트**
```
app/pet/check/page.tsx 에 문답 화면을 만들어줘.
문항은 PRD §8 F2의 가안 5개 + 체크 항목을 그대로 쓴다.
(W0 tally 결과는 아직 안 나왔다. T8에서 교체할 예정이므로
 문항 목록을 컴포넌트에 흩뿌리지 말고 한 곳에서 정의할 것)

- 1문 1화면. 진행률 표시
- 버튼 최소 48x48px
- 당뇨 "예" → 조건부 문항 분기
- 뒤로 가기 가능
- 답변은 서버로 보내지 않는다. 클라이언트 상태로만 유지

중요 — 자유 텍스트 입력을 만들지 마 (PRD §8 F2):
- 2번(물 외 섭취)은 룰셋 intake_categories 에서 선택지를 읽는다
- 3번(복용약)은 룰셋 medication_categories + 복용 시각
- "그 외"를 골라도 텍스트를 받지 않는다
```

**DoD**
- [ ] 5문항 완주에 40초 이내
- [ ] **자유 텍스트 입력 필드가 0개** — `<input type="text">` / `<textarea>` 검색 결과 없음
- [ ] 선택지가 룰셋에서 읽혀 나온다 (JSON에 항목을 추가하면 화면에 나타난다)

**커밋** — `feat: 상태 문답 플로우 (S3) — 잠정 문항`

---

#### T7. 요약카드 + 배지 (S4) ★ W1 안에 있어야 함

**목표** — 이 제품의 핵심 산출물. 직원이 3초 안에 스캔한다.

> **v1.0에서는 이 작업이 W2에 있었다.** 그 배치로는 W1 직원 시연 때 카드가 없어, PRD §8 F2의 "W1 종료 시점에 직원 피드백으로 확정"을 지킬 수 없었다. R4(요약카드를 직원이 안 봄)에 대한 유일한 대응이 실행되지 않는 구조였다.

**프롬프트**
```
lib/triage.ts 에 배지 판정 로직을, components/SummaryCard.tsx 에
요약카드를 만들어줘. PRD §8 F2 출력 예시를 그대로 재현할 것.

배지 판정은 PRD §9.4 를 따른다. 룰셋의 flags + triage 만으로 판정하고,
lib/triage.ts 에 검사별 조건문을 하드코딩하지 마.

  1. flags 중 level:"call" 해당      → call
  2. triage 중 level:"call" 해당     → call
  3. flags 또는 triage 중 tell 해당  → tell
  4. 없으면                          → ok

triage 가 없는 룰셋도 정상 동작해야 한다 (flags 만으로 판정).

문구는 반드시 룰셋의 levels 값을 쓴다. 하드코딩 금지.
"검사 가능/불가"라는 표현을 어떤 경우에도 생성하지 말 것.

카드 구조: 문장이 아니라 [항목: 값] 표.
주의가 필요한 항목만 하단에 다시 모아 표시.
```

**DoD**
- [ ] 세 배지(ok/tell/call) 모두 실제로 재현됨
- [ ] 카드 전체가 폰 화면 1스크롤 안에 들어감
- [ ] **룰셋 `triage`의 level 값만 바꿔서 배지가 바뀌는 것을 확인** (하드코딩 되지 않았다는 증거)
- [ ] 금지 표현 검색: `grep -rn "검사 가능\|검사 불가\|괜찮습니다\|문제없습니다" app/ lib/ components/` → 0건

**커밋** — `feat: 요약카드 및 배지 판정 (S4)`

---

> ### ⛳ W1 게이트 — 직원 시연
>
> **여기서 멈추고 직원 3명에게 보여준다. 이 게이트를 건너뛰지 않는다.**
>
> 이제 시연 대상에 **요약카드가 포함된다.** 이것이 v1.1에서 T7을 W1로 앞당긴 이유다.
>
> **물어볼 것**
>
> | 화면 | 질문 |
> |---|---|
> | 타임라인 | "이 화면 보고 온 환자면 접수가 빨라질 것 같나요?" |
> | 타임라인 | 금식 시각을 **02:00처럼 계산값 그대로** 주는 것이 실무 기준과 맞는지 |
> | **요약카드** | **"이 카드를 보면 뭘 안 물어봐도 되나요?"** ← 가장 중요한 질문 |
> | **요약카드** | 항목 순서. 직원이 **먼저 보고 싶은 것**이 맨 위여야 한다 |
> | **요약카드** | `triage` level 값 — 어떤 응답이 tell이고 어떤 것이 ok인지 (PRD §9.4) |
>
> 특히 `medication.taken`을 tell로 둘지 물어본다. 직원이 "혈압약은 어차피 안 물어본다"고 하면 ok로 내린다. **이 판단은 개발자가 하지 않는다.**
>
> **DoD**
> - [ ] 직원 피드백 3건 이상을 기록했다
> - [ ] 요약카드 항목 순서를 직원이 정한 대로 적었다
> - [ ] `triage` 각 규칙의 level 값을 직원과 함께 확정했다
>
> 반영 커밋은 T8에서 한다. 이 게이트의 산출물은 코드가 아니라 **확정된 결정 목록**이다.
>
> 이 과정에서 직원이 프로젝트의 공동 소유자가 된다. 그것이 R4에 대한 유일한 대응이다.

---

#### T8. 문답·카드 확정 반영 ★ W2 첫 작업

**선행 조건 2개**
- W0~W1 tally의 **9번 기타 원문 분석 완료**
- W1 게이트의 **직원 피드백 목록**

**목표** — 가안으로 만든 것을 실제 데이터와 실무 판단으로 교체한다.

**프롬프트**
```
두 가지 입력을 반영해줘.

1. W0 tally 9번 기타 원문 분석 결과 → 문답 문항 교체
   [여기에 분석 결과를 붙여넣는다]

2. W1 직원 피드백 → 요약카드 항목 순서 + 룰셋 triage level 값
   [여기에 확정 목록을 붙여넣는다]

규칙:
- triage level 은 lib/rules/f18-fdg-pet.json 만 수정한다.
  lib/triage.ts 코드는 건드리지 않는다
- 문항이 5개를 넘으면 40초 기준을 못 맞춘다. 늘리지 말고 교체해라
- 자유 텍스트 입력은 여전히 만들지 않는다
```

**DoD**
- [ ] 문답 문항이 tally 원문 근거로 교체되었다
- [ ] 카드 항목 순서가 직원이 정한 순서와 일치한다
- [ ] **`lib/triage.ts` 는 변경되지 않았다** (`git diff` 로 확인 — JSON만 바뀌어야 한다)
- [ ] 여전히 40초 이내 완주

**커밋** — `feat: W0 tally 및 직원 피드백 반영 (문답·카드 확정)`
> 커밋 본문에 **어떤 tally 항목**과 **누구의 피드백**이 근거인지 남긴다. 나중에 "이 문항이 왜 있지?"를 추적할 수 있어야 한다.

---

#### T9. URL 상태 인코딩

**목표** — 서버 저장 없이 카드를 재접근·공유 가능하게 만든다.

**프롬프트**
```
lib/encode.ts 에 문답 응답 + 예약일시를 URL-safe 문자열로
인코딩/디코딩하는 함수를 만들어줘.

- 압축해서 짧게 (카톡 링크 길이 고려)
- 개인 식별 정보는 애초에 없으므로 암호화 불필요
- 자유 입력 원문은 존재하지 않는다. 카테고리 코드만 인코딩된다
- 디코딩 실패 시 S1으로 안전하게 fallback
- 잘못된 파라미터로 진입해도 절대 크래시하지 않을 것
```

**DoD**
- [ ] 카드 URL을 다른 기기에서 열어도 동일하게 표시된다
- [ ] 인코딩된 문자열에 **한글 원문이 들어 있지 않다** (카테고리 코드만)
- [ ] 파라미터를 임의로 훼손해도 S1으로 fallback (크래시 없음)

**커밋** — `feat: URL 상태 인코딩으로 서버리스 공유 지원`

---

#### T10. 전달 기능 (공유 · 캘린더 · TTS)

> PRD v1.1 §8 F3에 Web Share API 사용과 fallback 규정이 반영되었다. (v1.0에서는 카카오 SDK가 병기되어 있었다)

**프롬프트**
```
세 가지 전달 기능을 만들어줘.

1. 공유: Web Share API (navigator.share).
   미지원 시 URL 복사 버튼으로 fallback + 복사 완료 토스트
2. 캘린더: lib/ics.ts — 금식 시작 시각과 도착 시각 2개 이벤트를 담은
   .ics 파일 생성 및 다운로드. 알림은 각 이벤트 시점 기준
   .ics 의 시각은 KST 기준으로 정확히 찍혀야 한다
3. 읽어주기: Web Speech API(SpeechSynthesis)로 타임라인 낭독.
   버튼은 타임라인 상단, ▶/⏸ 토글. lang="ko-KR"

   중요: speechSynthesis.getVoices() 로 한국어 음성 존재를 먼저 확인하고,
   없으면 버튼 자체를 렌더링하지 마.
   눌러도 소리가 안 나는 버튼은 고령 사용자에게 "고장난 서비스"로 읽힌다
```

**DoD**
- [ ] iOS Safari와 Android Chrome 양쪽에서 3기능 모두 동작
- [ ] 공유 미지원 환경에서 URL 복사 fallback 확인
- [ ] **한국어 TTS 음성이 없는 기기에서 읽어주기 버튼이 나타나지 않음**
- [ ] .ics를 실제 캘린더 앱에 넣었을 때 시각이 어긋나지 않음

**커밋** — `feat: 공유 · 캘린더 · 읽어주기 기능`

---

> ### ⛳ W2 게이트 — 본인 완주 테스트
>
> 본인 폰에서 QR 스캔 → 요약카드까지 **40초 이내 완주**. 3회 반복해서 평균으로 확인.
> 넘으면 화면 수를 줄인다. 기능을 늘리지 않는다.

---

#### T11. 익명 이벤트 로그

**목표** — v2 대시보드와 QI 발표의 재료를 쌓는다. **v1에서 대시보드는 만들지 않는다.**

> **착수 시 확인:** PRD §12는 저장소로 "Vercel KV"를 지목했으나, Vercel의 KV 제품은 마켓플레이스 연동 방식으로 바뀌어 왔다. **작업 시작 시점의 현행 제품명과 무료 티어 한도를 먼저 확인하고 시작한다.** v1은 적재만 하고 대시보드가 없으므로, 키-값 저장소면 무엇이든 무방하다 — 여기서 시간을 쓰지 않는다.

**프롬프트**
```
app/api/log/route.ts 에 익명 이벤트 수집 엔드포인트를 만들어줘.
저장은 [착수 시 확인한 KV 저장소] 사용.

수집 (PRD §8 F4):
- 진입 시각 (시간대 단위로 절삭)
- 예약 시각과의 상대 거리 (D-3, D-1, D-0)
- 문답 응답 분포 (카테고리 코드)
- 최종 배지
- 이탈 지점

절대 저장 금지: IP, User-Agent 원문, 기기 식별자, 정확한 예약 일시

문답이 선택지 방식이므로 자유 입력 원문은 애초에 존재하지 않는다.
룰셋에 정의된 카테고리 코드만 적재한다.
```

**DoD**
- [ ] 저장된 레코드를 직접 열어보고 **개인 식별 가능 정보가 0건**임을 눈으로 확인
- [ ] 로그 전송 실패가 사용자 화면을 깨뜨리지 않음 (fire-and-forget)
- [ ] 네트워크를 끊고도 타임라인·카드가 정상 동작

**커밋** — `feat: 익명 이벤트 로그 수집`

---

#### T12. 품질 점검

**목표** — PRD §13 비기능 요구사항 충족.

**체크리스트**
- [ ] Lighthouse 모바일 성능 — 첫 화면 1.5초 이내 (3G 스로틀링)
- [ ] **웹폰트가 0개** — 시스템 폰트만 사용 (한글 웹폰트로는 1.5초를 못 맞춘다)
- [ ] 본문 17px / 타임라인 시각 20px 이상
- [ ] 터치 영역 48×48px 이상
- [ ] 명도 대비 4.5:1 이상
- [ ] 배지가 색 + 아이콘 + 텍스트 3중 표기 (색맹 대응)
- [ ] `Disclaimer` 컴포넌트가 **모든 화면** 하단에 존재 (S1·S2·S3·S4 전부)
- [ ] iOS Safari 15 / Android Chrome 실기기 확인
- [ ] 금지 표현 grep 0건
- [ ] **kill switch 동작 확인** — `NEXT_PUBLIC_SERVICE_STATUS=maintenance` 로 전 화면 대체
- [ ] `TZ=UTC`, `TZ=America/New_York` 에서도 계산 결과 동일

**커밋** — `fix: 접근성 및 성능 기준 충족`

---

#### T13. 배포 준비물

**선행 조건 (PRD §19)** — 이것 없이는 QR을 만들 수 없다
- [ ] **서비스 정식 명칭 확정**
- [ ] **도메인 확정 및 연결** — QR 이미지가 도메인을 담으므로, 나중에 바꾸면 인쇄물을 전부 다시 만든다

**목표** — 내일 대기실에 붙일 수 있는 상태.

**작업**
- QR 이미지 생성 (`/pet` 링크, 최소 3cm × 3cm 인쇄 기준)
- 접수처·대기실 스티커 시안
  - 문구 예: **"검사 준비, 미리 확인하세요 — 1분"**
  - "AI", "서비스", "앱" 같은 단어를 쓰지 않는다. 고령 환자에게 진입 장벽이 된다
- 부착 위치 후보 3곳 선정 (접수 창구 정면 / 대기 의자 정면 / 안내지 배부 지점)

**DoD** — 인쇄해서 실제로 스캔해 봤다 (조명 낮은 대기실 기준)

**커밋** — `chore: QR 및 스티커 시안 추가`

---

> ### ⛳ W3 게이트 — 대기실 관찰
>
> 실제 환자 **10명 옆에서 말없이 관찰한다.** 도와주지 않는다.
>
> 기록할 것:
> - 스캔까지 걸린 시간 / 스캔 실패 횟수
> - 어느 화면에서 멈칫하는가
> - 보호자가 대신 하는 비율
> - 완주 여부
>
> 여기서 나오는 관찰 3~5건이 W4~W7 수정의 전부다. 이 관찰 없이 추측으로 고치지 않는다.

---

## 4. 트랙 C — 승인 (W3, W8)

### C1. W3 관리자 1차 보고

**목적은 승인이 아니라 예고다.** 여기서 승인을 받으려 하지 않는다.

**가져갈 것**
1. W0~W1 tally 결과 (주당 반복문답 N건, 1인당 평균 N초)
2. 폰으로 프로토타입 직접 시연 (자료 아님, 실물)
3. **리스크 관리 한 장** — 개인정보 미수집 / 의료판단 미수행 / 병원 브랜드 미사용 / **오류 시 즉시 중단 수단 보유(kill switch)**
4. 요청: "4주간 접수처 스티커로 시험해 보고 결과를 보고드리겠습니다"

3번에 kill switch를 넣는 이유: 부서장이 가장 두려워하는 것은 "잘못된 정보가 나갔을 때 멈출 수 있느냐"다. "환경변수 하나로 즉시 전 화면을 안내 문구로 바꿉니다"는 한 문장이 승인 확률을 크게 바꾼다.

**얻어야 할 것**
- 스티커 부착 구두 동의
- 안내지 출력 템플릿의 **소유 부서 정보** (PRD §19 미해결 항목)

### C2. W8 QI 발표

PRD §15 보고 자료 구성 5단계를 따른다. 순서를 바꾸지 않는다.

**3번(결과)은 두 문장으로 나눠 말한다** — PRD v1.1의 KPI 구조가 이것을 위한 것이다.

| | 말할 것 | 근거 |
|---|---|---|
| 효과 | "요약카드를 가져온 환자는 문답이 N초 → M초로 줄었습니다" | Primary KPI (tally [B]의 카드 O 레코드) |
| 한계 | "다만 스티커로는 도달률이 N%에 그쳤습니다" | Secondary — 카드 도달률 |
| 요청 | "그래서 안내지에 QR을 넣어 주십시오" | 위 두 문장의 결론 |

**전체 평균 단축률은 도달률을 넘을 수 없다**(PRD §4.4). 이 산술을 발표에서 먼저 밝히면, 낮은 전체 평균 수치가 실패가 아니라 **"개입은 유효하고 유통 경로가 병목"** 이라는 근거가 된다. 이것이 QR 삽입 요청의 논리 그 자체다.

**핵심:** 관리자를 움직이는 것은 3번(결과)이 아니라 **4번(리스크 관리)** 이다. 부서장이 가장 두려워하는 것은 업무 부하가 아니라 사고다.

**최종 요청은 하나로 좁힌다** — "안내지에 QR 1개 삽입". 예약일시 mail-merge는 이번에 요청하지 않는다. 두 개를 같이 올리면 검토가 길어진다.

---

## 5. Claude Code 세션 운영 규칙

1. **한 세션 = 한 작업(T번호).** 작업이 끝나면 `/clear`. 세션을 길게 끌면 앞서 정한 원칙이 흐려진다.
2. **세션 시작 시 첫 마디는 항상** — `PRD.md 와 WORKFLOW.md 를 읽고 T{n} 작업을 시작해줘`
3. **T3(계산 엔진)만은 테스트 먼저.** 다른 작업은 구현 우선이어도 무방하다.
4. **금지 표현 grep을 커밋 전 습관으로.**
   ```bash
   grep -rn "검사 가능\|검사 불가\|괜찮습니다\|문제없습니다\|판단됩니다" app/ lib/ components/
   ```
5. **룰셋 JSON을 코드에서 수정하지 않는다.** 값이 틀렸으면 JSON을 고친다. 로직에 예외를 넣기 시작하면 확장성이 죽는다.
   - 특히 `triage` level 값은 **JSON에서만** 바꾼다. `lib/triage.ts` 에 조건문이 늘어나기 시작하면 검사 확장이 불가능해진다.
6. **자유 텍스트 입력 필드를 만들지 않는다.** 문답 선택지는 룰셋에서 읽는다. 원문이 생성되는 순간 URL·공유·로그 세 곳에서 동시에 문제가 된다.
7. **기능 추가 요청이 떠오르면 코드가 아니라 PRD §19에 적는다.** W1~W3 동안 범위를 늘리지 않는다.

### 브랜치 · 커밋

- 브랜치: `main` 직접 커밋 (1인 개발, 리뷰어 없음). 실험은 `spike/*`로 분리 후 폐기.
- 커밋 메시지: `feat:` `fix:` `docs:` `chore:` `test:`
- **룰셋 JSON을 바꾼 커밋은 본문에 근거를 남긴다** — 어느 안내지 몇 년 몇 월판인지. 나중에 "이 값이 왜 6시간이지?"를 추적할 수 있어야 한다.

---

## 6. 배포 전 최종 체크리스트

스티커를 붙이기 직전에 한 번에 확인한다.

**안전**
- [ ] 경계값 테스트 10종 전부 통과
- [ ] `TZ=UTC` / `TZ=America/New_York` 에서도 통과
- [ ] 손으로 계산한 값과 3건 대조 완료
- [ ] 금지 표현 grep 0건
- [ ] Disclaimer가 모든 화면에 존재 (S1·S2·S3·S4)
- [ ] 저장된 로그에 개인 식별 정보 0건
- [ ] **kill switch 동작 확인** — 껐다 켜 본다
- [ ] 자유 텍스트 입력 필드 0개

**동작**
- [ ] iOS Safari / Android Chrome 실기기 완주
- [ ] `?t=` 파라미터 유무 양쪽 동작
- [ ] 카드 URL을 타 기기에서 열었을 때 동일 표시
- [ ] 공유 · 캘린더 · TTS 3기능 (또는 fallback) 동작
- [ ] 잘못된 URL 파라미터로 진입 시 크래시 없이 S1 fallback

**측정 준비** ← 이게 빠지면 W8에 증명할 것이 없다
- [ ] tally 시트에 **"카드" O/X 칸**이 있는 v1.1 버전인지 확인
- [ ] 창구 담당자가 after 측정에서 카드 O/X를 기록한다는 것을 알고 있음
- [ ] before 데이터 약 50건이 이미 손에 있음

**현장**
- [ ] 인쇄한 QR을 대기실 조명에서 스캔 성공
- [ ] 직원 3명이 요약카드를 3초 안에 읽는 것을 확인
- [ ] 관리자 구두 동의 확보

---

## 7. 중단 · 방향 전환 기준

계속 밀어붙이면 안 되는 신호를 미리 정해 둔다. 감정이 아니라 기준으로 판단하기 위해서다.

| 신호 | 시점 | 판단 |
|---|---|---|
| W3 대기실 관찰에서 **10명 중 1명 이하만 스캔** | W3 | 서비스가 아니라 **부착 위치·문구 문제**. 코드 수정 중단하고 스티커만 3종 A/B |
| 직원이 요약카드를 **보지 않고 그냥 질문함** | W4 | 카드 형식 실패. 직원과 다시 앉아 카드를 새로 그린다 (R4) |
| 시각 계산 오류가 **실환자에게 1건이라도 발생** | 언제든 | **즉시 kill switch ON** (`NEXT_PUBLIC_SERVICE_STATUS=maintenance`). 스티커는 그대로 두고 화면만 안내 문구로 바꾼다. 원인 규명 전까지 해제 금지 |
| W7까지 Primary KPI가 **전혀 안 움직임** | W7 | 정직하게 보고한다. 데이터를 만지지 않는다. **단, 카드 지참자 표본이 몇 건인지 먼저 본다** — 10건 미만이면 "효과 없음"이 아니라 "측정 불가"다. 그 경우 도달률 문제로 보고한다 |
| 관리자가 스티커 부착도 거부 | W3 | 개인 도구로 전환. 본인 담당 환자에게만 구두 안내로 계속 검증 |

---

## 부록 A. CLAUDE.md 초안

`CLAUDE.md`로 저장하면 Claude Code가 매 세션 자동으로 읽는다. T0에서 생성한다.

```markdown
# PET Time

FDG PET 검사 준비 안내 웹서비스. 상세 스펙은 PRD.md 참조.

## 타협 불가 원칙

1. **개인정보를 받지 않는다.**
   이름·등록번호·연락처·생년월일 입력 필드를 어떤 이유로도 추가하지 않는다.
   **자유 텍스트 입력 필드 자체를 만들지 않는다.** 문답 선택지는 룰셋에서 읽는다.
   원문이 생기는 순간 URL·공유·로그 세 곳에서 동시에 문제가 된다.

2. **의료 판단을 하지 않는다.**
   다음 표현을 생성하지 않는다:
   "검사 가능/불가", "괜찮습니다", "문제없습니다", "판단됩니다",
   "~일 수 있습니다"로 끝나는 추정 문장.
   출력은 룰셋 levels의 행동 지시 3종(ok/tell/call)뿐이다.

3. **모든 시각은 내림(floor) 처리한다.**
   금식 시작·복약 마지노선 → floor_hour / 도착 시각 → floor_10min.
   이르게 틀리는 방향으로만. 반올림 금지.
   **새벽 보정을 하지 않는다.** 계산값을 그대로 쓰고, 실행 조언이 필요하면
   룰셋 fasting.note 를 보조 한 줄로 붙인다.

4. **모든 계산은 KST 고정이다.**
   new Date() / getHours() 등 기기 로컬 타임존을 따르는 API를 계산 경로에 쓰지 않는다.
   룰셋 timezone 필드가 유일한 기준이다.

5. **병원 브랜드를 쓰지 않는다.**
   병원명·로고를 서비스 아이덴티티로 사용하지 않는다.
   연락처는 사실 정보로만 표기.

6. **모든 화면 하단에 Disclaimer를 넣는다.** (S1·S2·S3·S4 전부)
   "이 서비스는 병원 공식 서비스가 아니며, 검사 가능 여부를 판단하지 않습니다.
    최종 확인은 반드시 핵의학과 직원에게 받으세요."

## 아키텍처 규칙

- v1은 서버 상태를 갖지 않는다. 계산은 전부 클라이언트.
- 검사별 차이는 `lib/rules/*.json`으로만 표현한다. 코드에 검사별 분기를 넣지 않는다.
- 배지 판정은 룰셋 flags + triage 로만 한다. lib/triage.ts 에 조건문을 늘리지 않는다.
- 문구는 룰셋에서 읽는다. 컴포넌트에 하드코딩하지 않는다.
- 웹폰트를 추가하지 않는다. 시스템 폰트만.
- DB를 추가하지 않는다. 익명 로그만 KV.
- kill switch(NEXT_PUBLIC_SERVICE_STATUS)는 계산 로직보다 먼저 평가되어야 한다.

## 커밋 전 확인

grep -rn "검사 가능\|검사 불가\|괜찮습니다\|문제없습니다" app/ lib/ components/
grep -rn "<input type=\"text\"\|<textarea" app/ components/    # 0건이어야 함
npm test
```

---

## 부록 B. 주차별 요약

| 주 | 트랙A 측정 | 트랙B 개발 | 트랙C 승인 |
|---|---|---|---|
| **W0** | tally 착수 (동료 3명) | — | — |
| **W1** | tally 계속 (2주차) | T0~T7 → **직원 시연 게이트**<br>(타임라인 + **요약카드**) | — |
| **W2** | tally 분석 → 문답 확정 | T8~T10 → **40초 완주 게이트** | — |
| **W3** | — | T11~T13 → **대기실 관찰 게이트** | **1차 보고** + 스티커 배포 |
| **W4** | after 측정 시작 (워밍업)<br>**카드 O/X 기록 시작** | 관찰 기반 수정 | — |
| **W5~7** | after 측정 | 운영 · 소규모 수정 | 템플릿 소유 부서 협의 |
| **W8** | before/after 분석<br>(카드 지참자 기준) | — | **QI 발표 → QR 삽입 요청** |

**W1이 가장 빡빡하다.** T0~T7 여덟 작업이 한 주에 들어간다. 밀리면 T8~T10을 줄이지 말고 **W1 게이트를 W2 초로 미룬다** — 직원 피드백 없이 진행하는 것이 일정 초과보다 나쁘다.

---

## 부록 C. PRD 대비 변경 사항

v1.1에서 아래 항목이 **PRD.md v1.1에 모두 반영 완료**되었다. 두 문서 사이에 미반영 차이는 현재 없다.

| # | 항목 | 워크플로우 판단 | PRD 반영 |
|---|---|---|---|
| 1 | 카카오톡 공유 | Web Share API만 사용 | ✅ §5 · §7.1 · §8 F3 |

### v1.1 워크플로우 자체 변경

| # | 항목 | v1.0 | v1.1 | 사유 |
|---|---|---|---|---|
| 1 | 작업 번호 | T0~T12 | **T0~T13** (재배열) | 요약카드를 W1로 이동 |
| 2 | 요약카드 위치 | W2 (T7) | **W1 (T7)** | PRD §8 F2가 "W1 직원 피드백으로 확정"인데 W1에 카드가 없었다. R4 대응 공백 |
| 3 | T8 신설 | — | **문답·카드 확정 반영** | tally 결과 + 직원 피드백을 한 작업으로 묶음 |
| 4 | T6 선행 조건 | "W0 tally 분석 완료" | **삭제** (잠정 문항으로 구현) | 선행 조건을 유지하면 카드가 W2로 밀린다 |
| 5 | T3 경계값 | 8종, `00:30 → 전전날` | **10종**, `00:30 → 전날 18:00` | v1.0 기대값이 틀렸다. 전전날로 넘어가는 케이스는 없다 |
| 6 | T3 새벽 보정 테스트 | 포함 | **제거** | 규칙 자체가 PRD에서 삭제됨 |
| 7 | T3 타임존 | "서머타임 없음 확인" | **기기 타임존 무관 검증** | 실제 위험은 DST가 아니라 기기 타임존 |
| 8 | kill switch | 없음 | **T1에 포함** | R5 대응 "즉시 중단"에 수단이 없었다 |
| 9 | vitest 설치 | 어느 작업에도 없음 | **T1** | T3이 "테스트 먼저"인데 러너가 없었다 |
| 10 | 도메인·명칭 | 기한 없는 미해결 항목 | **T13 선행 조건** | QR 이미지가 도메인을 담는다 |
| 11 | tally [B] | "하루 5명" (주체 모호) | **창구 담당자 1명이 5건** + **카드 O/X 칸** | 표본 수가 3배까지 달라졌고, O/X 없이는 새 KPI를 계산할 수 없다 |
| 12 | 트랙A 기간 표기 | "W0, W4~W7" | **"W0~W1 수집"** | before 수집은 2주다 |

변경 사항이 추가되면 이 표에 누적하고, PRD 개정 시 일괄 반영한다.
