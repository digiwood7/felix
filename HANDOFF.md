# HANDOFF — 코드·디자인을 함께 만져보려는 사람에게

> ## 보낸 메시지
>
> 협업자를 초대할 때 보낸 안내다. 다음 사람에게도 그대로 쓸 수 있게 남겨 둔다.
>
> ---
>
> 저장소 초대 보냈어요. 수락하면 보일 거예요.
> https://github.com/Felix-Bak/pet-time
>
> 작업은 이렇게 부탁드려요:
> 포크 뜨셔서 본인 저장소에서 하시고, 다 되면 PR 하나 보내주세요.
> 제가 확인하고 머지합니다.
>
> 클론하시면 루트에 HANDOFF.md 가 있어요. 그거 먼저 읽어주세요.
> 설치·실행법부터 어디를 만져도 되는지까지 다 적어놨습니다.
> 환경변수 설정 같은 거 없이 `npm install && npm run dev` 하면 바로 떠요.
>
> 한 가지만 미리 말씀드리면 —
> 이게 지금 실제로 돌아가는 서비스예요. 병원에 QR 스티커가 붙어 있고
> 검사 전날 밤에 진짜 환자가 이 화면을 봅니다.
> 그래서 원칙이 좀 빡빡한 게 있어요 (웹폰트 금지, 자유 입력 필드 금지 같은).
> HANDOFF.md 6번에 여섯 개로 정리해놨으니 그것만 보고 시작하시면 됩니다.
>
> 대신 포크에서 하시는 건 배포랑 완전히 분리돼 있어서
> 마음껏 부수고 실험하셔도 아무 일 안 일어나요. 편하게 보셔도 됩니다.
>
> 디자인 쪽으로 손볼 데가 많다고 생각하고 있어요.
> `docs/red-team.md` 에 "환자가 이해 못 하는 것" 목록이 있는데
> 거기 디자인으로 풀리는 게 여럿 있어요. 참고하시면 좋을 것 같습니다.
>
> 막히거나 이상하면 편하게 연락 주세요.

---

이 저장소를 받아서 **코드를 고치거나 디자인을 다시 해보려는 사람**이 읽는 문서다.
무엇을 해도 되고 무엇을 하면 안 되는지, 작업한 것을 어떻게 돌려주는지가 여기 다 있다.

---

## 0. 먼저, 이건 이미 돌아가는 서비스다

`https://petct.kr/pet` 로 배포되어 있고, **QR 스티커가 이미 병원 벽에 붙어 있다.**
검사 전날 밤에 이 화면을 여는 사람은 실제 환자다.

그래서 배포 경로를 하나로 좁혀 두었다:

```
당신의 포크  ──PR──▶  Felix-Bak/pet-time 의 main  ──▶  Vercel  ──▶  petct.kr
                      (여기 머지하는 사람은 저장소 주인 한 명)
```

**당신이 무엇을 하든 petct.kr 에는 반영되지 않는다.** 포크에서 아무리 부수고 실험해도
안전하다. 반대로 말하면, 반영되게 하는 방법도 PR 하나뿐이다.

Vercel 프로젝트·도메인 계정·`.env.local`(로그 저장소 토큰)은 넘기지 않는다.
없어도 개발에는 아무 지장이 없다 — 아래 3번 참고.

---

## 1. 작업 방식 — 포크하고, PR 로 돌려준다

1. GitHub 에서 이 저장소를 본인 계정으로 **Fork** 한다 (원본이 private 이므로 포크도 private)
2. 포크를 clone 해서 브랜치를 하나 판다 — `design/summary-card` 처럼 뭘 하는지 보이는 이름
3. 작업하고 커밋한다
4. 원본 저장소로 **Pull Request** 를 연다
5. 저장소 주인이 확인하고 머지한다. 배포는 그때 일어난다

원본 저장소의 `main` 에 직접 push 하지 않는다. 애초에 권한이 없다.

---

## 2. 커밋 메시지

접두사를 붙인다: `feat:` `fix:` `docs:` `chore:` `test:`

기존 커밋들이 **"무엇을 바꿨는지"가 아니라 "왜 바꿨는지"** 를 한 줄로 적고 있다.
`git log` 를 한 번 훑어보면 톤이 잡힌다. 굳이 흉내 낼 필요는 없지만,
디자인을 바꿨다면 **무엇이 불편해서 바꿨는지**는 적어주는 편이 리뷰가 빠르다.

---

## 3. 실행하기

```bash
git clone <당신의 포크 주소>
cd pet-time
npm install
npm run dev          # http://localhost:3000
```

Node 20 이상 (개발은 24 에서 하고 있다). 그 외 준비물은 없다.

**환경변수 없이 그냥 돌아간다.** `.env` 파일을 만들 필요가 없다.
`.env.example` 에 두 가지가 적혀 있는데 둘 다 개발에는 필요 없다:

| 변수 | 비워두면 |
|---|---|
| `NEXT_PUBLIC_SERVICE_STATUS` | 정상 동작 (`maintenance` 로 두면 전 화면이 점검 안내로 바뀐다 — 그 화면을 보고 싶을 때만 쓴다) |
| `UPSTASH_REDIS_REST_*` | 익명 로그가 조용히 버려진다. 화면은 그대로 동작한다 |

### 폰에서 보기

같은 Wi-Fi 라면 `npm run dev` 뒤 터미널에 뜨는 `Network:` 주소를 폰에서 열면 된다.
Next 16 은 기본적으로 다른 오리진의 CSS·JS 를 막는데, `next.config.ts` 가
현재 기기의 사설 IP 를 자동으로 허용 목록에 넣어 준다 — 손댈 필요 없다.

다른 네트워크에 있는 사람에게 보여줘야 하면:

```bash
cloudflared tunnel --url http://localhost:3000
```

`*.trycloudflare.com` 도 이미 허용되어 있다.

---

## 4. 화면 지도

| 라우트 | 화면 | 하는 일 |
|---|---|---|
| `/` | **S1** | 예약 날짜·시각·장소 입력. `?t=` 가 붙어 오면 건너뛰고 S2 로 보낸다 |
| `/pet` | **S2** | 역산 타임라인 (금식 시작 / 도착 / 검사) |
| `/pet/check` | **S3** | 상태 문답 — 선택지는 전부 룰셋에서 읽는다 |
| `/pet/card` | **S4** | 요약카드 + 배지(ok / tell / call) |
| `/maintenance` | — | kill switch 가 켜졌을 때의 화면 |

`docs/` 안에는 화면이 아닌 인쇄물·검토용 HTML 이 있다 (`sticker.html`, `staff-review.html`,
`tally-sheet.html`). 디자인 대상에 포함해도 되지만 우선순위는 S1~S4 가 훨씬 높다.

---

## 5. 만져도 되는 곳 / 만지면 안 되는 곳

### 마음껏 만져도 되는 곳

```
app/globals.css          색·타이포·간격의 뿌리
app/layout.tsx           전체 골격
components/*.tsx         각 화면 조각의 마크업과 클래스
docs/sticker.html        인쇄물
```

Tailwind v4 를 쓴다 (`@theme` 블록이 `globals.css` 안에 있다).
설정 파일이 따로 없는 게 정상이다.

### 손대기 전에 물어봐야 하는 곳

```
lib/triage.ts            배지 판정
lib/schedule.ts          시각 역산
lib/koreanTime.ts        KST 계산
lib/rules/*.json         룰셋 — 시간·문구·선택지의 원본
proxy.ts                 CSP·보안 헤더
next.config.ts
```

이쪽은 **의료 안내의 정확성에 직접 걸려 있다.** 특히 `lib/rules/*.json` 은 값 하나가
병원 안내지 몇 년 몇 월판에서 온 것인지 커밋 본문에 근거를 남기게 되어 있다.
버그를 발견했다면 고치지 말고 **이슈나 PR 설명에 적어달라.** 그게 훨씬 도움이 된다.

---

## 6. 깨뜨리면 안 되는 것 — 디자인하다 보면 반드시 걸린다

`CLAUDE.md` 에 전부 적혀 있지만, 화면을 만지는 동안 실제로 부딪히는 건 이 여섯 개다.

**① 자유 텍스트 입력 필드를 만들지 않는다.**
`<input type="text">` 도 `<textarea>` 도 안 된다. 이름·등록번호·연락처·생년월일은
어떤 이유로도 받지 않는다. 문답 선택지는 룰셋에서 읽는다.
원문이 생기는 순간 URL·공유·로그 세 곳에서 동시에 문제가 된다.

**② 웹폰트를 추가하지 않는다.**
한글 웹폰트는 서브셋을 해도 수백 KB라 성능 기준을 못 맞추고, 로딩 중 글자가
사라지거나 튀는 게 고령 사용자에게 치명적이다. 시스템 폰트 스택은 `globals.css`
`@theme` 에 이미 잡혀 있다. 자간·행간·크기·굵기로 승부해야 한다.

**③ 본문 17px 아래로 내리지 않는다.**
`html { font-size: 106.25% }` 로 잡혀 있다. 16px 로 되돌리면 조밀해 보이지만
기준선을 깬다.

**④ 모든 화면 하단의 Disclaimer 를 지우지 않는다.**
S1·S2·S3·S4 전부에 들어간다. 레이아웃을 정리하다 제일 먼저 사라지는 게 이것이다.
문구는 `lib/disclaimer.ts` 한 곳에만 있다.

**⑤ 문구를 컴포넌트에 하드코딩하지 않는다.**
표시되는 말은 룰셋(`lib/rules/*.json`)에서 온다. 문구가 어색하면 JSON 을 고치거나
PR 설명에 적어달라. 컴포넌트 안에 문자열을 새로 심으면 검사 종류가 늘어날 때 무너진다.

**⑥ 의료 판단처럼 읽히는 말을 만들지 않는다.**
"검사 가능/불가", "괜찮습니다", "문제없습니다", "판단됩니다", "~일 수 있습니다"로
끝나는 추정 문장 — 전부 금지어다. `npm run check` 가 실제로 잡아낸다.
출력은 행동 지시 3종(ok / tell / call)뿐이다.

병원 **로고·색상**은 쓰지 않는다. 연락처는 사실 정보로만 표기한다.

병원명은 S1 히어로 맨 윗줄 한 줄(`삼성서울병원`)에만 있다. **여기를 늘리지
말아 달라** — 아직 병원 승인을 받은 표기가 아니고, 하단 Disclaimer("병원 공식
서비스가 아니며")와 이미 부딪치는 중이다 (`docs/red-team.md` #15).

---

## 7. 외부 리소스는 전부 차단된다 (CSP)

`proxy.ts` 가 엄격한 CSP 를 붙인다. 실제로 걸리는 건 이렇다:

- ❌ CDN 에서 폰트·아이콘·CSS·JS 불러오기 — `default-src 'self'` 에 막힌다
- ❌ 인라인 `<script>` — nonce 없이는 실행되지 않는다
- ✅ 인라인 style 과 style 속성 — `style-src` 에 `'unsafe-inline'` 이 있다
- ✅ npm 으로 설치해서 import 하는 것 — 번들에 들어가므로 `'self'` 다

라이브러리를 쓰고 싶으면 `<script src>` 가 아니라 `npm install` 로 넣는다.
다만 **의존성을 늘리는 PR 은 이유를 적어달라** — 이 프로젝트는 런타임 의존성이
next·react·react-dom 셋뿐이고, 그게 의도된 상태다.

---

## 8. PR 보내기 전에 반드시

```bash
npm run check
```

한 번에 네 가지를 본다 — 타입 검사, 테스트, **타임존 3종**(UTC / New York / Kiritimati),
금지 표현 검사. 모든 계산이 KST 고정이어야 하므로 기기 타임존을 바꿔가며 돌린다.

그리고 자유 텍스트 필드가 들어가지 않았는지:

```bash
grep -rn "<input type=\"text\"\|<textarea" app/ components/
```

**0건이어야 한다.**

`npm run check` 가 통과하지 않는 PR 은 머지되지 않는다. 뭔가 실패하는데 원인을
모르겠으면 그대로 PR 을 열고 출력을 붙여달라 — 같이 본다.

---

## 9. 실제 URL 로 보고 싶다면

`localhost` 말고 진짜 주소에서 확인하고 싶으면, **본인 Vercel 무료 계정에 본인 포크를
연결**하면 된다. `something.vercel.app` 주소가 따로 생기고 petct.kr 과는 아무 관계가 없다.
환경변수는 넣지 않아도 된다 (3번 표 참고).

---

## 10. 더 읽을 것

| 문서 | 언제 읽나 |
|---|---|
| `CLAUDE.md` | 지금. 짧다. 원칙의 원본이다 |
| `PRD.md` | 왜 이 화면이 이렇게 생겼는지 궁금할 때 — 기능 명세·룰셋·계산 규칙 |
| `WORKFLOW.md` | 어디까지 왔고 무엇이 남았는지 (T0~T13) |
| `docs/red-team.md` | **여기가 재밌다** — 어디서 깨지는지, 환자가 뭘 이해 못 하는지 열린 항목이 표로 있다. 디자인으로 풀 수 있는 게 여럿이다 |

---

## 마지막으로

이 서비스의 목적은 **안내문 디지털화가 아니라 접수 창구의 반복 문답을 줄이는 것**이다.
환자가 병원에 오기 전에 자기 준비 상태를 스스로 정리해 오게 만드는 게 전부다.

그래서 "예뻐 보이는가"보다 **"검사 전날 밤 11시에 노안이 온 60대가 침대에서
30초 안에 읽히는가"** 가 기준이다. 그 기준으로 지금 화면이 부족한 곳이 있다면,
그게 정확히 당신이 손대야 할 곳이다.
