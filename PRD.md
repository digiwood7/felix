# PET Time — 제품 요구사항 정의서 (PRD)

| | |
|---|---|
| **문서 버전** | v1.1 |
| **작성일** | 2026-08-08 (v1.1 개정 2026-08-10) |
| **제품 가칭** | PET Time |
| **대상 검사** | F-18 FDG PET/CT (Fusion Torso) |
| **상태** | v1 스펙 확정 · 개발 착수 전 |
| **개정 이력** | [부록 C](#부록-c-v11-개정-내역) 참조 |

---

## 1. 한 줄 정의

> **환자가 병원에 도착하기 전에 자신의 검사 준비 상태를 스스로 정리해 오게 만들어, 핵의학과 접수 단계의 반복 문답을 줄이는 웹 서비스.**

이것은 안내문 디지털화가 아니다. **핵의학과 반복업무 감소 솔루션**이다.

---

## 2. 문제 정의

### 2.1 현상

환자는 외래에서 PET 검사 안내지를 받는다. 외래는 환자 수와 검사 종류가 많아 개별 설명이 어렵고, 안내지 한 장으로 갈음한다. 환자는 검사 당일 핵의학과에 도착해서야 안내지에 이미 적힌 내용을 구두로 묻는다. 직원은 같은 설명을 하루에 수십 번 반복한다.

### 2.2 원인 분석

"환자가 안내지를 읽지 않는다"는 증상이지 원인이 아니다. 다섯 개의 구조적 원인이 겹쳐 있다.

| # | 원인 | 설명 |
|---|---|---|
| 1 | **시점 불일치** | 안내지는 D-7~D-30에 전달되나, 정보가 필요한 순간은 D-1 저녁(금식 시작)과 D-day 새벽(복약 판단)이다. 그 순간 종이는 손에 없다. |
| 2 | **번역 미제공** | "검사 전 6시간 금식"은 규칙이지 지시가 아니다. 08:25 예약 → 02:25부터 금식이라는 계산을 환자가 직접 해야 한다. 환자의 질문은 "6시간이 뭐냐"가 아니라 "그래서 내가 몇 시부터 굶느냐"이다. |
| 3 | **정보 과잉** | 한 장에 금식·운동·약·혈당·소요시간·폐쇄공포증·임신·결과안내가 모두 있다. 환자는 "그래서 내가 할 일이 뭐냐"를 찾지 못한다. |
| 4 | **불안 요인** | FDG PET 대상자는 암 진단·재발 평가 환자다. 불안 상태에서는 읽어도 저장되지 않고, 답을 알아도 사람에게 확인받고 싶어 한다. 정보 개선만으로 완전히 사라지지 않는다. |
| 5 | **비용/편익 분리** | 설명 생략의 이득은 외래가, 비용은 핵의학과가 100% 부담한다. 외래에 개선 동기가 구조적으로 없다. |

### 2.3 재정의된 문제

> 환자가 검사 준비 정보를 못 받은 것이 문제가 아니라, **환자별 상황으로 번역된 지시가 필요한 시점에 확인 가능한 형태로 전달되지 않아** 반복 문답·검사 지연·재예약·직원 부담이 발생하는 것이 본질이다.

### 2.4 진짜 병목 — 설명이 아니라 캐묻기

"물 마셔도 되나요?"에 답하는 데는 3초가 걸린다. 실제 시간을 소모하는 것은 이쪽이다.

```
직원: 언제부터 금식하셨어요?
환자: 어제 저녁 먹고요.
직원: 몇 시쯤이요?
환자: 글쎄... 8시? 9시쯤?
직원: 그 뒤로 드신 거 없으세요?
환자: 물만... 아 약은 먹었어요.
직원: 무슨 약이요?
환자: 혈압약이랑... 여보, 그거 뭐였지?
```

**환자는 자기 상태를 구조화해서 진술할 능력이 없다.** 아파서가 아니라 원래 사람이 그렇게 말하지 않는다.

따라서 이 제품이 만들어야 할 산출물은 안내가 아니라 **구조화된 상태 진술서(요약카드)** 이다. 나머지 기능은 전부 이 카드를 만들기 위한 부속이다.

---

## 3. 목표와 비목표

### 3.1 목표

- 접수 단계의 준비사항 문답 시간을 단축한다.
- 환자가 도착 전에 준비 실패를 스스로 발견하게 한다.
- 병원의 승인·심의 절차 없이 배포 가능한 형태를 유지한다.
- 핵의학과 관리자가 QI 활동으로 채택할 수 있는 데이터를 남긴다.

### 3.2 비목표 (명시적으로 하지 않는 것)

| 하지 않는 것 | 이유 |
|---|---|
| EMR / OCS / HIS 연동 | 개인 개발자 접근 불가. 연동을 전제하면 프로젝트가 시작되지 않는다. |
| 개인정보 수집 (이름·등록번호·연락처·생년월일) | 수집하는 순간 개인정보 심의 대상이 되어 배포가 6개월 늦어진다. |
| 검사 가능 여부 판정 | 의료행위 영역. 오판 1건에 프로젝트가 아니라 개인이 책임진다. |
| 병원 로고·명칭을 서비스 브랜드로 사용 | 무단 사용 시 즉시 중단 사유가 된다. |
| 안내지 OCR / 병원명 인식 | 문제는 인식이 아니라 워크플로우 병목이다. 복잡도만 늘린다. |
| FDG 외 검사 (골스캔, 심근, PSMA 등) | v1 범위 밖. 단 룰셋 구조는 확장 가능하게 설계한다. |
| 자유 대화형 AI 챗봇 | 환각 리스크 대비 효용이 낮다. 정형 문답 5개로 대부분이 커버된다. |
| 음성 입력 (STT) | v2로 이연. 사유는 §8.5 참조. |

---

## 4. 성공 지표

### 4.1 Primary KPI (단 하나)

> **요약카드를 제시한 환자의 접수 준비사항 문답 소요 시간(초)**

- 측정: 환자가 창구에 선 순간 → 준비사항 확인이 끝난 순간
- 비교: **before 전체 평균** vs **after 요약카드 제시 환자**
- 목표: **before 대비 50% 이상 단축**
- 기록 방법: tally 시트 [B]에 **요약카드 제시 여부(O/X)** 를 함께 표시한다 (§16)

**왜 "전체 평균"이 아니라 "카드 제시 환자"인가**

전체 평균을 Primary로 걸면 지표가 도달률에 갇힌다. §4.4의 산술 관계 때문이다.

```
전체 평균 단축률의 상한 = 도달률
→ 도달률 18%에서는 카드 제시 환자의 문답 시간이 0초여도
   전체 평균은 18%를 넘어 단축될 수 없다.
```

지표를 분리하면 W8 발표에서 두 문장을 각각 증명할 수 있다.

1. **"카드는 효과가 있다"** — 카드 제시 환자 180초 → 30초 (Primary)
2. **"이제 도달률만 올리면 된다"** — 현재 18%, 안내지 QR 삽입 시 대폭 상승 예상 (Secondary)

이것이 §15 "안내지 QR 정식 삽입" 요청의 논리 구조 그 자체다. 전체 평균 하나로는 이 논리를 만들 수 없다.

### 4.2 Secondary (관찰만, 목표치 없음)

- **전체 평균 문답 시간** — 실제 업무 부하 감소분. 단축률 상한이 도달률이라는 점을 함께 보고한다
- **요약카드 도달률** — 접수 환자 중 카드 제시 비율. 안내지 QR 삽입 요청의 핵심 근거
- 문의 유형별 건수 변화
- 준비 미흡으로 인한 지연·취소 건수
- QR 스캔 → 요약카드 생성 완료율 (서비스 내부 로그)

### 4.3 KPI에서 제외한 항목과 사유

| 제외 항목 | 사유 |
|---|---|
| 검사 취소·재예약 감소 | base rate가 낮아(일 1~2건) 4주를 모아도 노이즈와 구분되지 않는다. |
| 환자 만족도 | 설문 배포는 사실상 심의 영역. 회수율도 확보되지 않는다. |
| 검사 시작 지연 감소 | 장비·주사·판독 등 교란변수가 압도적이다. |

### 4.4 도달률에 대한 현실 인식

전체 설계는 "QR을 찍는다"는 전제 위에 서 있고, 그 전제가 가장 약하다. 낙관적 추정:

```
안내지 수령 환자                    100명
  ├─ QR 존재를 인지                 ~50명
  ├─ 실제 스캔                      ~25명
  ├─ 요약카드까지 완료               ~18명
  └─ 직원 문의가 실제로 줄어든 사람   ~12명
```

**전체 평균에 대한 산술 제약**

```
after 전체 평균 = 도달률 × (카드 제시 환자 시간)
                + (1 - 도달률) × (before 평균)

전체 평균 단축률 = 도달률 × (카드 제시 환자의 단축률)
                ≤ 도달률
```

before 180초, 도달률 18% 기준:

| 카드 제시 환자 시간 | 전체 평균 | 전체 단축률 |
|---|---|---|
| 30초 | 153초 | 15% |
| 0초 (이론적 하한) | 147.6초 | **18%** |

**전체 평균 기준 20% 단축은 도달률 18%에서 수학적으로 달성 불가능하다.** 카드 제시 환자의 문답 시간이 0초여도 안 된다. 이것이 §4.1에서 Primary KPI를 카드 제시 환자 기준으로 정의한 이유다.

전체 평균은 Secondary로 관찰하되, **목표치를 걸지 않는다.** 목표를 걸면 성공한 개입이 실패로 보고된다.

---

## 5. 사용자

핵심 통찰: **QR을 찍는 손은 70대 환자의 손이 아니라 40~50대 보호자의 손인 경우가 많다.**

| 역할 | 주체 | 특성 | 설계 함의 |
|---|---|---|---|
| **입력자** | 보호자 (40~50대) 또는 환자 본인 | 스마트폰 능숙, 불안도 높음, 대신 챙겨줌 | 입력 UI는 일반 성인 기준. 과도한 고령자 UI는 오히려 정보 밀도를 떨어뜨림 |
| **행동자** | 환자 (60~70대) | 금식하고, 약 먹고, 시간 맞춰 도착 | 결과물(타임라인)은 큰 글씨 + TTS. 카톡으로 전달받아 볼 수 있어야 함 |
| **판독자** | 핵의학과 직원 | 10초 안에 상황 파악해야 함 | 요약카드는 스캔 가능한 구조. 문장이 아니라 표 |
| **구매자** | 핵의학과 관리자 | 부하 감소 + 리스크 없음을 원함 | 승인 자료의 절반은 기능이 아니라 리스크 관리 항목 |

**설계 결론:** v1의 필수 기능에 **공유 버튼**이 포함된다. 보호자가 타임라인을 만들어 부모에게 전송하는 것이 실제 확산 경로다. 공유된 링크는 앱 설치도 QR 재스캔도 필요 없다.

구현은 **Web Share API**를 쓴다. 모바일에서 네이티브 공유 시트를 열면 그 안에 카카오톡이 이미 들어 있다. 카카오 JS SDK는 개발자 등록 + 도메인 화이트리스트가 필요해 3주 일정에 맞지 않는다.

---

## 6. 설계 원칙 (타협 불가)

1. **개인 식별 정보를 일절 받지 않는다.** 입력은 검사종류 + 날짜 + 시각 + 상태 문답뿐이다. 이 조합으로는 개인 식별이 불가능하다.
2. **의료 판단을 하지 않는다.** 서비스의 출력은 판정이 아니라 **행동 지시** 3종뿐이다. (§9.3)
3. **모든 시각 계산은 안전한 방향으로만 틀린다.** (§9.2)
4. **서버 없이 동작하는 것을 최대화한다.** 타임라인 계산은 전부 클라이언트에서 수행한다.
5. **병원 브랜드를 사용하지 않는다.** 병원명·로고·색상을 서비스 아이덴티티로 쓰지 않는다. 안내 문구 내 병원 연락처 표기는 사실 정보로만 허용.

---

## 7. 범위

### 7.1 v1 (3주)

- F1. 개인화 역산 타임라인
- F2. 상태 문답 5개 → 요약카드
- F3. 공유(Web Share API) · 캘린더(.ics) 등록 · 읽어주기(TTS)
- F4. 익명 이벤트 로그 (대시보드는 미포함, 적재만)

### 7.2 v2 (실증 데이터 확보 후)

- 음성 입력 (녹음 → STT → 구조화)
- 직원용 통계 대시보드
- 검사 종류 확장 (골스캔, 심근, PSMA, Amyloid, DAT)

### 7.3 명시적 제외 (당분간 하지 않음)

- 자유 대화형 챗봇
- 검사 리허설 / 실시간 병원 알림 / 푸시 알림 발송
- 회원가입 · 로그인 · 예약 조회

---

## 8. 기능 명세

### F1. 개인화 역산 타임라인 (핵심)

**입력:** 검사 날짜, 예약 시각 (검사 종류는 QR 경로로 고정)

**출력 예시** (2026-08-06 목 08:25 예약, 당뇨 없음 기준)

```
8월 5일 (수)
  종일    등산 · 헬스 · 골프 등 과격한 운동을 하지 마세요

8월 6일 (목)
  02:00   이 시각 이후 아무것도 드시지 마세요
          전날 저녁 식사 이후로 하시면 편합니다
          물(생수)만 가능 · ✕ 보리차 커피 우유 주스 껌 사탕
          같은 날 다른 검사가 있으면 접수처에 먼저 확인하세요

  04:00   당뇨약 · 인슐린을 쓰신다면 이 시각까지만 사용 가능합니다
          이후에 사용하셨다면 접수처에 꼭 말씀해 주세요

  08:00   암병원 지하 1층 핵의학과 도착
          접수 시 혈당을 측정합니다 · 안내지 기준은 200 이하입니다

  08:25   검사 시작 — 약 1시간 20분 소요
```

**당뇨 항목은 묻지 않고 항상 표시한다 (v1.2 변경)**

v1.0은 "당뇨 응답이 예인 경우 이 항목이 추가된다"였다. 그런데 당뇨 문항은 S3에 있고 이 타임라인은 S2다. **S2에서는 답을 알 수 없다.** 화면 흐름상 성립하지 않는 스펙이었다.

당뇨약 마지노선은 D-day 새벽에 필요한 가장 중요한 정보 중 하나이므로, 입력을 늘리는 대신 **조건부 문구("~쓰신다면")로 항상 표시**한다.

- 입력 0개. S2 도달까지의 마찰이 늘지 않는다
- 당뇨가 아닌 사람은 한 줄 읽고 넘어간다
- 당뇨인 사람은 묻지 않아도 시각을 얻는다

S3의 당뇨 문항은 그대로 유지된다. 요약카드에 "당뇨: 없음"을 적으려면 답이 필요하고, 그것이 접수처의 캐묻기를 줄이는 항목이기 때문이다.

**요구사항**

- 시각은 반드시 시:분으로 계산해 표기한다. "6시간 전"이라고 쓰지 않는다.
- 날짜는 요일을 함께 표기한다.
- 각 항목은 한 줄 지시문 + 보조 설명. **보조는 2줄까지, 금식 항목만 3줄까지 허용한다.**
  - v1.0은 "3줄을 넘기지 않는다"였으나, 금식 항목은 허용/금지 목록과 "같은 날 다른 검사" 단서가 본질적으로 붙어 4줄이 된다. 단서를 빼면 틀린 안내가 되므로 제한 쪽을 완화한다.
  - 최종 판단은 T4 화면 구성과 W1 직원 시연에서 한다.
- 검사 장소는 건물이 여러 곳이므로 **서비스가 추측하지 않는다.** S1에서 선택받은 건물을 표기한다.
- 금식 시각은 **룰셋 계산값을 그대로 표기한다.** 새벽 시각이라는 이유로 임의로 앞당기지 않는다. 대신 보조 설명 한 줄로 실행 가능한 조언("전날 저녁 식사 이후로 하시면 편합니다")을 덧붙인다. 이 문구는 룰셋 `fasting.note`에서 읽는다.

---

### F2. 상태 문답 → 요약카드

**문답 (v1 가안 — Week 0 tally 결과로 확정)**

| # | 질문 | 입력 형식 |
|---|---|---|
| 1 | 마지막으로 음식을 드신 시각은? | 시각 선택 |
| 2 | 물 외에 드신 것이 있나요? | 없음 / 있음 → **카테고리 선택** |
| 3 | 오늘 드신 약이 있나요? | 없음 / 있음 → **약 분류 선택** + 복용 시각 |
| 4 | 당뇨약이나 인슐린을 사용하시나요? | 예 → 4-1 분기 / 아니오 |
| 5 | 어제·오늘 과격한 운동을 하셨나요? | 예 / 아니오 |
| + | 해당되는 항목 체크 | 140kg 초과 / 폐쇄공포증 / 임신·수유 가능성 |

체크 항목은 룰셋 `flags`와 1:1로 일치해야 한다.

**혈당은 체크 항목이 아니다 (v1.2 변경)**

v1.0~v1.1은 `flags`에 "혈당 200 초과 이력"을 두었다. 잘못된 분류였다.

> **혈당은 당일 접수 시 병원이 전 환자에게 직접 측정하는 값이다.** 환자의 자가 응답으로 판정할 수 있는 항목이 아니다.

`flags`에 두면 이런 일이 생긴다.

```
환자가 "해당 없음" 선택  →  🟢 접수처에 추가로 말씀하실 내용이 없습니다
                         →  당일 측정 250  →  검사 진행 안 됨
                         →  "앱이 괜찮다고 했는데"
```

거짓 안심이다. Disclaimer가 법적으로는 막아주지만 체감은 막지 못하고, 이 프로젝트가 얻어야 할 신뢰를 정확히 반대로 깎는다.

**대신 도착 항목의 사실로 적는다** (`arrival.note`, §9.1).

```
08:00   암병원 지하 1층 핵의학과 도착
        접수 시 혈당을 측정합니다 · 안내지 기준은 200 이하입니다
```

- 전 환자 대상이므로 조건 없이 항상 표시한다
- **병원 기준을 사실로 인용할 뿐 이 환자를 판정하지 않는다.** §14의 "안내지에 명시된 사실의 재구성" 범위 안이다
- 미리 알고 가게 되므로 헛걸음의 충격이 줄어든다. v1.0에는 이 사실이 화면 어디에도 없었다

문답 4번(당뇨약·인슐린)이 사실상 같은 인구를 포착한다. 혈당 200을 넘어본 환자는 대부분 이미 당뇨 환자이므로, 별도 체크는 40초 화면에서 중복 질문이었다.

**같은 원칙:** 병원이 당일 측정·판단하는 값은 `flags`에 넣지 않는다. 환자가 스스로 아는 것만 묻는다.

**문답 5개와 체크 4개는 성격이 다르다 (v1.2 명시)**

| | 문답 1~5 | 체크 4개 |
|---|---|---|
| 성격 | 직원이 **모든 환자에게** 이미 묻는 것 (§2.4의 그 대화) | **드문 예외** 선별 |
| 줄이는 것 | 접수 문답 시간 → **Primary KPI** | 헛걸음·재예약 → §4.2 |
| 화면 | 1문 1화면, 5화면 | **1화면에 4개 체크박스** |
| 조작 | 5탭 | **1탭** ("없으면 그대로 다음") |

체크 4개를 1문 1화면으로 만들면 40초 기준을 못 맞춘다. 전부 해당 없는 환자가 대부분이므로 **기본값 미체크 + 한 번에 넘기기**로 만든다.

**이 4개가 정말 필요한지는 W0 tally로 검증한다.** 접수에서 실제로 묻지 않는 항목이라면 환자 시간만 쓰고 직원 시간은 아끼지 못한다. tally 결과 문의가 없으면 T8에서 문답에서 빼고 타임라인 안내 문구로 내린다.

**이 서비스는 확인의 총량을 줄이지 않는다**

줄이는 것은 **누가 언제 확인하는가**다.

```
지금:  직원이 창구에서 구두로 캐물음    약 3분   ← 병목 자원
이후:  환자가 도착 전에 탭으로 답함     40초    ← 비병목 자원
```

Primary KPI가 "질문 건수"가 아니라 "문답 소요 시간"인 이유가 이것이다(§4.1). 문답 5개는 새로 만든 질문이 아니라 §2.4에 적힌 그 대화를 그대로 옮긴 것이다.

그리고 **S3는 선택이다.** 타임라인(S2)만 보고 이탈해도 목적의 절반은 달성된다(§10). 문답은 요약카드를 원하는 사람만 한다.

**자유 입력을 쓰지 않는 이유 (v1.1 변경)**

2·3번은 당초 자유 입력이었으나 **선택지 방식으로 변경**한다.

- 문답 응답은 F3 공유를 위해 URL에 인코딩된다(§12). 자유 입력을 유지하면 **"혈압약" 같은 건강정보 원문이 URL에 실려 카카오톡 등 외부 메신저 서버를 경유**한다.
- 식별자가 없어 법적으로는 방어 가능하지만, "개인정보를 일절 받지 않는다"는 이 프로젝트의 **최대 무기가 흐려진다.** 관리자 승인 자료(§15-4)에서 해명해야 할 항목이 하나 늘어난다.
- 선택지 방식이면 URL·카드·로그 전부 카테고리 코드만 담게 되어 **원문이 애초에 생성되지 않는다.**
- 부수 효과: 고령 사용자의 텍스트 입력이 사라져 40초 완주 기준(§13)에 유리하다.

선택지는 룰셋 `medication_categories` / `intake_categories`에서 읽는다. 코드에 하드코딩하지 않는다.

**"그 외"를 고른 경우에도 텍스트를 받지 않는다.** 카드에는 "그 외 복용약 06:00"으로만 표기하고, 상세는 접수처에서 구두로 확인한다. 이 서비스의 역할은 **직원이 무엇을 물어야 하는지 미리 알려주는 것**이지 답을 다 받아내는 것이 아니다.

**요약카드 출력**

```
┌────────────────────────────────┐
│  FDG PET/CT · 8/6(목) 08:25 예약  │
│                                │
│  마지막 식사    8/5 21:00        │
│  물 외 섭취     없음             │
│  복용약        혈압약 06:00      │
│  당뇨          없음              │
│  전날 운동      없음              │
│                                │
│  🟡 접수하실 때 이 화면을         │
│     보여주세요                   │
│     · 혈압약 06:00 복용          │
└────────────────────────────────┘
```

**요구사항**

- 직원이 3초 안에 스캔할 수 있어야 한다. 문장이 아니라 항목:값 구조.
- 주의가 필요한 항목만 하단에 다시 모아 보여준다.
- 배지 판정은 **룰셋 `triage` 규칙으로만** 수행한다 (§9.4). 컴포넌트나 로직에 조건문을 하드코딩하지 않는다.
- 항목 순서·표현과 **`triage` 각 규칙의 level 값**은 **W1 게이트에서 직원 3명의 피드백으로 확정**한다. 개발자가 정하지 않는다.
  → 따라서 요약카드 구현(T7)은 **W1 안에 들어와야 한다.** 개발 순서 제약이므로 워크플로우와 반드시 일치시킨다.
- 카드는 화면 캡처 없이도 재접근 가능해야 한다 (URL에 상태 인코딩, 서버 저장 없음).

---

### F3. 전달 기능

| 기능 | 구현 | 목적 |
|---|---|---|
| 공유 | **Web Share API** (`navigator.share`) | 보호자 → 환자 전달. **실제 확산 경로.** 네이티브 공유 시트에 카카오톡이 포함되어 있다 |
| 캘린더 등록 | .ics 파일 생성 | 금식 시작 시각에 기기 자체 알림. 발송 서버 불필요 |
| 읽어주기 | Web Speech API (SpeechSynthesis) | 고령자는 읽기보다 듣기. 브라우저 내장, 구현 비용 최소 |

읽어주기 버튼은 타임라인 상단에 배치한다. 음성 *입력*은 어렵지만 음성 *출력*은 저비용 고효과다.

**미지원 환경 fallback (v1.1 추가)**

| 기능 | 미지원 시 동작 |
|---|---|
| 공유 | URL 복사 버튼으로 대체. 복사 완료 토스트 표시 |
| 캘린더 | 항상 동작 (파일 다운로드) |
| 읽어주기 | **한국어(ko-KR) 음성이 없으면 버튼 자체를 렌더링하지 않는다.** 눌러도 소리가 안 나는 버튼은 고령 사용자에게 "고장난 서비스"로 읽힌다 |

Android는 기기별로 TTS 엔진에 한국어 음성이 없을 수 있다. `speechSynthesis.getVoices()`로 사전 확인한다.

---

### F4. 익명 이벤트 로그

**적재 항목**

```
- 진입 시각 (일/시간대 단위)
- 예약 시각과의 상대 거리 (D-3, D-1, D-0 등)
- 문답 각 항목의 응답 분포
- 최종 배지 (ok / tell / call)
- 이탈 지점 (어느 화면에서 나갔는가)
```

**절대 적재하지 않는 항목:** IP, User-Agent 원문, 기기 식별자, 정확한 예약 일시

v1.1에서 문답 2·3번이 선택지 방식으로 바뀌었으므로(§8 F2) **자유 입력 원문이 애초에 생성되지 않는다.** 로그에는 룰셋에 정의된 카테고리 코드(`beverage_caffeine`, `med_bp` 등)만 적재된다.

v1에서는 대시보드를 만들지 않는다. 도달률 20% 수준에서는 볼 데이터가 없다. 4주 실증 후 로그를 근거로 v2에서 만든다.

---

### F5. (v2) 음성 입력 — v1 제외 사유 기록

차별화 임팩트는 크지만 v1의 최대 함정이다.

- 개발 기간이 3주 → 5~6주로 증가
- iOS Safari 음성인식 불안정
- 고령자 사투리·발음에 대한 브라우저 내장 인식률 저하
- **녹음 데이터는 생체정보 취급 논란 소지** → "개인정보 미수집"이라는 최대 무기가 흐려진다

4주 실증으로 숫자를 만든 뒤, 그 명분으로 v2에 추가한다. 그때는 승인 근거도 함께 생긴다.

---

## 9. 데이터 모델 및 계산 규칙

### 9.1 룰셋 JSON

검사 확장은 이 파일을 복사하는 것으로 끝난다. 코드는 수정하지 않는다.

```json
{
  "id": "f18-fdg-pet",
  "label": "PET/CT (F-18 FDG)",
  "timezone": "Asia/Seoul",
  "duration_min": 80,

  "locations": {
    "ask": "어느 건물에서 검사받으시나요?",
    "hint": "예약 안내문에 적혀 있습니다",
    "fallback_text": "지하 1층 핵의학과",
    "options": [
      { "id": "cancer", "label": "암병원", "text": "암병원 지하 1층 핵의학과" },
      { "id": "main", "label": "본관", "text": "본관 지하 1층 핵의학과" }
    ]
  },

  "arrival": {
    "before_min": 20,
    "round": "floor_10min",
    "text": "{location} 도착",
    "note": "접수 시 혈당을 측정합니다 · 안내지 기준은 200 이하입니다"
  },

  "exam": {
    "text": "검사 시작 — 약 {duration} 소요"
  },

  "fasting": {
    "hours": 6,
    "round": "floor_hour",
    "text": "이 시각 이후 아무것도 드시지 마세요",
    "note": "전날 저녁 식사 이후로 하시면 편합니다",
    "note_if_between": ["00:00", "06:00"],
    "allowed": ["물(생수)"],
    "allowed_note": "같은 날 다른 검사가 있으면 접수처에 먼저 확인하세요",
    "forbidden": ["보리차", "커피", "우유", "주스", "껌", "사탕"]
  },

  "conditional": [
    {
      "id": "diabetes",
      "ask": "당뇨약이나 인슐린을 사용하시나요?",
      "offset_h": -4,
      "round": "floor_hour",
      "text": "당뇨약 · 인슐린을 쓰신다면 이 시각까지만 사용 가능합니다",
      "after_text": "이후에 사용하셨다면 접수처에 꼭 말씀해 주세요"
    }
  ],

  "restrictions": [
    {
      "id": "exercise",
      "from": "prev_day_start",
      "display": "all_day",
      "text": "등산 · 헬스 · 골프 등 과격한 운동을 하지 마세요"
    }
  ],

  "intake_categories": [
    { "id": "beverage_caffeine", "label": "커피 · 차" },
    { "id": "beverage_sugar",    "label": "주스 · 음료" },
    { "id": "dairy",             "label": "우유 · 유제품" },
    { "id": "candy_gum",         "label": "사탕 · 껌" },
    { "id": "intake_other",      "label": "그 외" }
  ],

  "medication_categories": [
    { "id": "med_bp",       "label": "혈압약" },
    { "id": "med_diabetes", "label": "당뇨약 · 인슐린" },
    { "id": "med_thyroid",  "label": "갑상선약" },
    { "id": "med_other",    "label": "그 외" }
  ],

  "flags": [
    { "id": "weight",    "q": "체중이 140kg을 넘으시나요?",          "level": "call" },
    { "id": "claustro",  "q": "폐쇄공포증이 있으신가요?",             "level": "call" },
    { "id": "pregnancy", "q": "임신 가능성이 있거나 수유 중이신가요?", "level": "call" }
  ],

  "triage": [
    { "when": "fasting.broken",        "level": "tell", "label": "금식 시작 이후 음식 섭취" },
    { "when": "intake.non_water",      "level": "tell", "label": "물 외 섭취" },
    { "when": "medication.taken",      "level": "tell", "label": "당일 복약" },
    { "when": "diabetes.after_cutoff", "level": "call", "label": "당뇨약 마지노선 이후 사용" },
    { "when": "exercise.yes",          "level": "tell", "label": "전날 과격한 운동" }
  ],

  "levels": {
    "ok":   "접수처에 추가로 말씀하실 내용이 없습니다",
    "tell": "접수하실 때 이 화면을 보여주세요",
    "call": "출발 전에 1599-3114로 먼저 전화해 주세요"
  }
}
```

이 파일 안에 "검사 가능" / "검사 불가"라는 단어가 한 번도 등장하지 않는 것에 유의한다. 이것이 승인의 열쇠다.

**v1.0 대비 변경점**

구조 변경 (v1.1)

| 항목 | 변경 | 사유 |
|---|---|---|
| `timezone` | 신설 | KST 고정. §9.2 참조 |
| `arrival_before_min` → `arrival` | 객체화 + `round` 명시 | v1.0은 도착 시각의 내림 단위가 정의되지 않아, 시 단위로 내리면 10:50 예약 → 10:00 도착(50분 전)이 되었다 |
| `fasting.early_morning_shift` | **제거**, `note` 한 줄로 대체 | §9.2 참조 |
| `restrictions.from_h: -24` → `from` + `display` | 표현 방식 변경 | v1.0은 `-24h`인데 F1 출력은 "종일"이었다. 안내지 원문도 "검사 전일"이므로 전날 종일이 맞다 |
| `intake_categories` / `medication_categories` | 신설 | 자유 입력 제거 (§8 F2) |
| `triage` | **신설** | §9.4 참조 |

**값 정정 (v1.2) — 안내지 실물 대조 결과**

2026-08-10 안내지 대조에서 9개 항목 중 4개가 달랐다. 부록 B의 값은 PRD 작성 시점에 옮겨 적은 것이었고, 원본과 어긋나 있었다.

| 항목 | v1.0 (부록 B) | **안내지 원본** | 영향 |
|---|---|---|---|
| `duration_min` | 90 | **80** | 환자가 종료 시각을 40분 늦게 계획 |
| `weight` | 130kg 이상 | **140kg 초과** | 131~140kg 환자에게 불필요한 🔴 전화 배지. **접수처 부하를 오히려 늘린다** |
| `glucose` | `flags`의 체크 항목 | **`flags`에서 제거 → `arrival.note` 사실로** | 당일 병원이 전 환자에게 측정하는 값이다. 자가 응답으로 🟢 를 띄우면 거짓 안심이 된다. §8 F2 |
| `fasting.allowed_note` | 없음 | **"같은 날 다른 검사가 있으면 접수처에 먼저 확인"** | 룰셋에 통째로 누락. 아래 참조 |
| `location` → `locations` | 암병원 고정 | **암병원 / 본관** | 서비스가 건물을 추측하면 환자가 다른 건물까지 걸어갔다 온다 |

금식 6시간, 당뇨약·인슐린 4시간, 도착 20분 전, 연락처 1599-3114는 원본과 일치했다.

> **`allowed_note` 누락이 가장 위험했다.** 같은 날 복부초음파처럼 물도 금지인 검사가 잡혀 있으면, 서비스가 "물(생수)만 가능합니다"라고 **확신 있게 틀린 안내**를 하게 된다. 환자가 종이보다 이 화면을 믿으므로 종이 안내지보다 나쁘다.

**이 값들은 `lib/rules/rules.test.ts` 에서 런타임으로 고정되어 있다.** 값을 바꾸려면 테스트를 함께 고쳐야 하므로 조용히 바뀌지 않는다.

### 9.2 안전 불변조건

> **모든 시각은 내림(버림) 처리한다.**

| 대상 | 내림 단위 | 효과 |
|---|---|---|
| 금식 시작 시각 | `floor_hour` | 더 이르게 → 안전 |
| 복약 마지노선 | `floor_hour` | 더 이르게 → 안전 |
| 도착 시각 | `floor_10min` | 더 이르게 → 안전 |

반올림하지 않는다. 계산 오류가 발생해도 항상 안전한 방향으로만 틀린다.

**도착 시각만 10분 단위인 이유 (v1.1 변경)**
v1.0은 도착 시각의 내림 단위를 정의하지 않았다. 시 단위로 내리면 10:50 예약 → 10:30 → **10:00**이 되어 실제보다 50분 일찍 오게 만든다. 안전한 방향이긴 하나 환자를 대기실에 30분 더 앉혀 두는 것은 이 서비스가 만든 새로운 부담이다. 10분 단위면 최대 오차가 9분으로 제한된다.

**새벽 시각 보정 규칙은 v1.1에서 제거되었다**

v1.0에는 "계산된 금식 시작이 00:00~06:00이면 전날 자정으로 앞당겨 표시" 규칙이 있었으나 삭제한다.

| 문제 | 내용 |
|---|---|
| 값이 문서마다 달랐다 | F1 예시 `21:00` / 룰셋 `"전날 자정"` / §9.2 `"전날 저녁 식사 이후"` — 세 값이 모두 달랐고 21:00은 어떤 규칙에서도 도출되지 않았다 |
| "전날 자정"이 모호했다 | 8/6 예약에서 "8/5 자정"이 `8/5 00:00`이면 **32시간 금식**이 된다 |
| 경계에서 점프가 생긴다 | 06:00 예약 → 계산값 `00:00` → 보정 시 24시간이 통째로 앞당겨진다 |
| 근거가 없다 | 안내지 원문에 없는 값을 서비스가 만들어낸 것이며, 이는 "안내지에 명시된 사실의 재구성"(§14)이라는 방어선을 벗어난다 |

**대신 계산값을 그대로 표기하고, 룰셋 `fasting.note` 한 줄을 보조 설명으로 덧붙인다.**

```
02:00   이 시각 이후 아무것도 드시지 마세요
        전날 저녁 식사 이후로 하시면 편합니다   ← fasting.note
```

시각은 정확하게 주고, 실행 가능한 조언은 보조 문구로 분리한다. 보조 문구는 판정이 아니라 권유이므로 §11 금지 표현에 걸리지 않는다.

**계산 예시** (2026-08-06 목 08:25 예약)
```
금식 6h  → 8/6 02:25 → floor_hour  → 8/6 02:00
당뇨 4h  → 8/6 04:25 → floor_hour  → 8/6 04:00
도착 20m → 8/6 08:05 → floor_10min → 8/6 08:00
운동     → restrictions.from: prev_day_start → 8/5 종일
```

**타임존 (v1.1 추가)**

모든 계산은 **KST(Asia/Seoul) 고정**이다. `new Date()`나 `getHours()`처럼 **실행 기기의 로컬 타임존을 따르는 API를 계산 경로에 사용하지 않는다.**

한국은 서머타임이 없으므로 DST 전환은 고려 대상이 아니다. 실제 위험은 **사용자 기기의 타임존이 KST가 아닌 경우**다.

- 해외 체류 중인 보호자가 링크를 열어 타임라인을 만들어 보낸다 → 전 항목이 어긋난다
- 기기 타임존이 수동으로 잘못 설정되어 있다

룰셋의 `timezone` 필드를 유일한 기준으로 삼는다.

### 9.3 출력 레벨 — 판정이 아닌 행동 지시

| 배지 | ❌ 쓰지 않는 표현 | ✅ 사용하는 표현 |
|---|---|---|
| 🟢 ok | 검사 가능 | 접수처에 추가로 말씀하실 내용이 없습니다 |
| 🟡 tell | 확인 필요 | 접수하실 때 이 화면을 보여주세요 |
| 🔴 call | 검사 불가 소지 | 출발 전에 1599-3114로 먼저 전화해 주세요 |

한 글자도 판단하지 않으면서 실무 효과는 동일하다.

### 9.4 triage — 배지 판정 규칙 (v1.1 신설)

v1.0에는 **배지 판정을 유발하는 조건이 룰셋에 없었다.** `level`을 가진 것은 `flags` 4개뿐인데, §8 F2의 카드 예시는 "혈압약 06:00 복용" 때문에 🟡 tell이 떠 있었다. 이 규칙은 어디에도 정의되어 있지 않았고, 그대로 구현하면 다음 원칙이 무너진다.

> "검사별 차이는 룰셋 JSON으로만 표현한다. 코드에 검사별 분기를 넣지 않는다."

**판정 순서**

```
1. flags 중 level:"call" 이 하나라도 해당       → 🔴 call
2. triage 중 level:"call" 이 하나라도 해당      → 🔴 call
3. flags 또는 triage 중 level:"tell" 이 해당    → 🟡 tell
4. 해당 없음                                   → 🟢 ok
```

**`when` 키 정의** — 코드가 해석하는 조건식이다. 조건 판정과 등급을 분리해, 값(level)만 JSON에서 바꾸면 되도록 한다.

| `when` | 판정 조건 |
|---|---|
| `fasting.broken` | 문답 1의 마지막 식사 시각 > 계산된 금식 시작 시각 |
| `intake.non_water` | 문답 2가 "있음" |
| `medication.taken` | 문답 3이 "있음" |
| `diabetes.after_cutoff` | 문답 4가 "예" **이고** 복용 시각 > 당뇨 마지노선 |
| `exercise.yes` | 문답 5가 "예" |

**level 값은 W1 게이트에서 직원 3명의 피드백으로 확정한다.** 위 표의 값은 가안이다. 특히 `medication.taken`을 tell로 둘지는 접수 실무에 따라 갈린다 — 직원이 "혈압약은 어차피 안 물어본다"고 하면 ok로 내린다.

`triage`가 비어 있으면 배지는 `flags`만으로 판정되며, 이는 유효한 상태다. (검사 종류 확장 시 triage 없는 룰셋도 동작해야 한다)

---

## 10. 화면 흐름

```
QR 스캔
  │
  ▼
[S1] 날짜 · 시각 · 건물 입력    ← URL 파라미터 있으면 자동 채움
  │                              (?t=202608060825, 개인정보 아님)
  ▼
[S2] 타임라인                  ← 여기까지가 최소 가치. 이탈해도 성공
  │   ▶ 읽어주기 / 캘린더 / 공유
  │
  ▼  "검사 준비 확인하기"
[S3] 문답 5개 + 체크 1화면      ← 1문 1화면, 큰 버튼
  │
  ▼
[S4] 요약카드 + 배지            ← 접수처에 제시하는 화면
      ▶ 공유 / 캡처 안내
```

**중요:** S2에서 이탈해도 이 서비스는 이미 목적의 절반을 달성한 것이다. S3 진입을 강제하지 않는다.

**S2까지 필요한 입력은 3개뿐이다** — 날짜, 시각, 건물.

금식 시작·도착 시각·운동 제한은 전부 **예약 시각만으로 계산된다.** 당뇨 항목도 묻지 않고 조건부 문구로 표시한다(§8 F1). 문답 5개는 요약카드(S4)를 원하는 사람만 한다.

건물은 계산이 아니라 표기에만 쓰이지만, 틀리면 환자가 다른 건물로 걸어가므로 추측하지 않고 묻는다.

---

## 11. 문구 가이드

### 금지 표현

- "검사 가능합니다" / "검사할 수 없습니다" / "재예약이 필요합니다"
- "괜찮습니다" / "문제없습니다" — 판단으로 읽힌다
- "~일 수 있습니다" 로 끝나는 모든 추정 문장
- 병원명·로고를 서비스 주체처럼 표기하는 일체의 표현

### 필수 표기

- **모든 화면 하단**: **"이 서비스는 병원 공식 서비스가 아니며, 검사 가능 여부를 판단하지 않습니다. 최종 확인은 반드시 핵의학과 직원에게 받으세요."**
  - 첫 화면만이 아니다. 공유 링크로 들어온 사람은 S2나 S4가 첫 화면이 되고, 요약카드는 **직원에게 보여주는 화면**이므로 여기에 문구가 없으면 안 된다.
- 입력 화면: **"이름·등록번호 등 개인정보는 입력받지 않으며, 어떤 정보도 서버에 저장되지 않습니다."**

이 두 문장은 환자를 위한 것이기도 하지만, **관리자 승인 자료의 핵심 슬라이드**이기도 하다.

---

## 12. 기술 스택

| 영역 | 선택 | 사유 |
|---|---|---|
| 프레임워크 | Next.js (App Router) | Claude Code 활용도가 가장 높음 |
| 호스팅 | Vercel | 기존 숙련도 |
| DB | **v1에서는 사용하지 않음** | 계산은 전부 클라이언트. 익명 로그는 서버리스 함수 + KV로 충분 |
| 상태 전달 | URL 파라미터 인코딩 | 서버 저장 없이 재접근·공유 가능 |
| 음성 출력 | Web Speech API | 브라우저 내장, 무료 |
| 스타일 | Tailwind | — |

**Flutter Web을 쓰지 않는 이유:** QR을 찍는 환경은 병원 대기실 와이파이 + 3~4년 된 안드로이드다. 초기 번들이 무거워 첫 화면이 3초 이상 걸리면 환자는 그냥 직원에게 물어본다. 문제를 해결하려다 재생산하게 된다.

**v1에서 DB를 빼는 이유:** 스키마 설계에 이틀을 쓰는 대신 그 시간을 요약카드 디자인에 쓰는 편이 성과가 크다.

---

## 13. 비기능 요구사항

| 항목 | 기준 |
|---|---|
| 첫 화면 표시 | 3G 환경에서 **1.5초 이내** |
| 완주 시간 | S1 → S4까지 **40초 이내** |
| 본문 글자 크기 | 최소 17px, 타임라인 시각은 20px 이상 |
| 터치 영역 | 최소 48×48px |
| 명도 대비 | WCAG AA (4.5:1) 이상 |
| 색상 의존 금지 | 🟢🟡🔴 배지는 색 + 아이콘 + 텍스트를 함께 제공 |
| 지원 브라우저 | iOS Safari 15+, Android Chrome 100+ |
| 오프라인 | 불필요 (단, 로드 후에는 네트워크 없이 계산 동작) |
| 웹폰트 | **사용하지 않는다.** 시스템 폰트만 사용 |

**웹폰트 금지 (v1.1 추가):** 한글 웹폰트는 서브셋을 해도 수백 KB다. 3G에서 1.5초 기준을 맞출 수 없고, 폰트 로딩 중 텍스트가 사라지거나(FOIT) 튀는 현상은 고령 사용자에게 치명적이다. iOS·Android 모두 기본 한글 폰트가 충분히 읽힌다.

---

## 14. 개인정보 및 법적 검토

| 쟁점 | 대응 |
|---|---|
| 개인정보 수집 | 이름·등록번호·연락처·생년월일 일절 미수집. 검사종류+일시만으로는 식별 불가 |
| URL 파라미터 | `?t=202608060825` (일시만). 개인 식별자를 절대 포함하지 않음 |
| 문답 응답의 URL 인코딩 | 요약카드 공유를 위해 응답이 URL에 실린다. **선택지 코드만 담기며 자유 입력 원문은 존재하지 않는다** (§8 F2) |
| 건강정보 외부 경유 | 공유된 URL은 메신저 서버를 경유한다. 카테고리 코드(`med_bp` 등)만 담기므로 원문 노출이 없고, 식별자가 없어 특정 개인과 결합되지 않는다 |
| 의료기기 해당성 | 진단·판정 기능 없음. 안내지에 명시된 사실의 재구성 및 시각 계산만 수행 |
| 병원 브랜드 | 미사용. 병원 연락처는 사실 정보로만 표기 |
| 안내지 QR 삽입 | 병원 공식 문서 변경이므로 **정식 승인 필요** (§15) |

---

## 15. 배포 전략 — 승인이 크리티컬 패스

배포 경로로 **안내지 QR 정식 삽입**을 선택했다. 이 경우 프로젝트의 병목은 개발이 아니라 승인이다. 코드는 3주면 나오지만 승인은 6주가 걸릴 수 있다.

따라서 두 트랙을 병렬로 진행한다.

```
Week 0 ─── tally 시작 (동료 3명) ────────────────────┐
                                                     │
Week 1~3 ── 개발 ─────────────────────────────────┐  │
                                                  │  │
Week 3 ──────────── 프로토타입 + tally 데이터 → 관리자 1차 보고
                                                     │
Week 3~4 ── 접수처 스티커로 선행 배포 (승인 불필요)      │
                                                     │
Week 4~7 ── 실증 + 승인 절차 병행                     │
                                                     │
Week 8 ──── QI 발표 → 안내지 QR 정식 삽입
```

**스티커 선행 배포는 QR 삽입의 대체가 아니라 예행연습이다.** 스캔률·이탈 지점·문답 응답 분포를 실측해, 정식 삽입 요청서에 "예상 도달률 N%" 대신 **"측정된 도달률 N%"**를 쓸 수 있게 된다.

### 관리자 보고 자료 구성 (Week 8)

1. 문제 — tally 데이터 (주당 반복문답 N건, 1인당 평균 N초)
2. 개입 — QR + 요약카드, 화면 3컷
3. 결과 — 문답 시간 X초 → Y초
4. **리스크 관리 — 개인정보 미수집 / 의료판단 미수행 / 병원 브랜드 미사용**
5. 요청 — 안내지 QR 정식 삽입

관리자를 움직이는 것은 3번이 아니라 **4번**이다.

### 안내지 템플릿 변경 요청

- 사전 확인 필요: 출력 템플릿의 소유 부서 (전산팀 / 핵의학과 / 진료협력)
- 요청 범위 1단계: QR 이미지 1개 삽입
- 요청 범위 2단계(선택): 예약일시를 URL 파라미터로 mail-merge — 기술적으로는 필드 1개 추가 수준
- 2단계는 1단계 승인 후 별도로 요청한다. 한꺼번에 올리면 검토가 길어진다.

---

## 16. 측정 계획 (Week 0)

개발보다 먼저 시작한다. 이 데이터가 없으면 어떤 결과가 나와도 증명할 수 없다.

관찰자가 3명이므로 **"반복질문"의 판단 기준이 사람마다 갈리는 것이 최대 리스크**다. 판단 여지를 없앤 설계로 간다.

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  PET 검사 준비 문의 집계   날짜 __/__ (__)   관찰자 ____
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

 [A] 환자가 물어본 것
     ※ 물어본 것만 표시. 내가 먼저 설명한 것은 제외

  1. 금식 — 언제부터 / 뭘 먹었는데 되나       ____________
  2. 물 · 음료 — 물, 커피, 보리차, 껌, 사탕    ____________
  3. 당뇨약 · 인슐린                         ____________
  4. 그 외 복약 — 혈압약 등                   ____________
  5. 운동                                    ____________
  6. 검사 소요시간 / 언제 끝나나               ____________
  7. 결과 언제 나오나 / 어디서 듣나            ____________
  8. 도착시간 · 장소                          ____________
  9. 그 외 → 뒷면에 환자 말 그대로 받아적기

 [B] 접수 문답 시간   ※ 창구 담당자가 하루 5명. 첫 환자부터 순서대로
     창구에 선 순간 → 준비사항 확인이 끝난 순간
     카드 = 환자가 요약카드 화면을 보여줬으면 O (W0에는 전부 X)

          시간      카드
     ①  ____초    O / X
     ②  ____초    O / X
     ③  ____초    O / X
     ④  ____초    O / X
     ⑤  ____초    O / X

 [C] 오늘 준비 미흡으로 지연 · 취소된 건        ___건
     사유: ________________________________
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 운영 규칙

1. **[B]는 하루 5명, 첫 환자부터 순서대로.** "복잡해 보이는 환자"를 고르면 데이터가 죽는다. 무작위성이 자동 확보되는 규칙이 필요하다.
   - **관찰자 3명 전체가 아니라 그날 창구 담당자 1명이 5건을 기록한다.** 하루 5건 × 10영업일 = before 약 50건. 3명이 각자 5건씩 재면 같은 환자가 중복 기록된다.
   - [A] 문의 유형 집계는 3명 전원이 각자 기록한다.
2. **[B]의 "카드" 칸은 v1.1에서 추가되었다.** 이 칸이 없으면 W8에 Primary KPI(§4.1)를 계산할 수 없다. W0에는 서비스가 없으므로 전부 X이지만, **before/after 시트를 동일하게 유지해야 하므로 W0부터 넣는다.**
3. **9번 기타는 반드시 원문 그대로.** 요약하면 정보가 사라진다. **이것이 문답 5개를 확정하는 재료다.**
4. **before/after는 같은 요일끼리 비교.** 화요일이 붐빈다면 화요일-화요일로 본다.
5. 3명이 동일한 시트를 쓰고, 시작 전 5분간 항목 해석을 맞춘다.

### 동료 협조 요청 화법

"새 서비스를 만들려고요"보다 **"우리가 하루에 같은 설명을 몇 번 하는지 세어보려고요"**로 시작한다. 숫자를 본 순간 동료들이 먼저 문제의식을 갖는다. 그것이 나중에 관리자 앞에서 실무자 지지로 돌아온다.

---

## 17. 개발 일정

| | 산출물 | 완료 기준 |
|---|---|---|
| **W1** | 룰셋 JSON + 시각 계산 엔진 + 타임라인 + 문답 + **요약카드** | **직원 3명에게 시연하고 표현·항목·triage level 수정 반영** |
| **W2** | 문답 확정 반영 + URL 인코딩 + 공유 · .ics · TTS | 본인 폰에서 처음부터 끝까지 40초 내 완주 |
| **W3** | 익명 로그 + 스티커 시안 + 관리자 보고자료 | 대기실에서 실제 환자 10명 옆에서 관찰 |

W1의 완료 기준이 "코드 동작"이 아니라 "직원 피드백 반영"인 것은 의도적이다. 요약카드의 항목 순서는 직원이 정해야 하고, 그 과정에서 직원이 이 프로젝트의 공동 소유자가 된다.

**v1.1 변경 — 요약카드를 W1로 앞당겼다.** v1.0은 요약카드를 W2에 두면서 §8 F2에서는 "W1 종료 시점에 직원 피드백으로 확정"하라고 했다. W1 시연 때 보여줄 카드가 없어 **R4(요약카드를 직원이 안 봄)에 대한 유일한 대응책이 실행 계획에서 빠져 있었다.**

이 때문에 W1의 문답(T6)은 W0 tally 확정 전 **잠정 문항**으로 만든다. tally 결과와 직원 피드백은 W2 첫 작업(T8)에서 함께 반영한다.

---

## 18. 리스크

| # | 리스크 | 영향 | 대응 |
|---|---|---|---|
| R1 | QR 스캔률이 예상보다 낮음 | 높음 | 스티커 선행 배포로 실측 후 QR 위치·문구 개선. 도달률 자체를 개선 대상으로 관리 |
| R2 | 안내지 QR 삽입 승인 지연·거부 | 높음 | 스티커 배포로 실증 데이터 확보 후 재요청. QI 발표를 승인 근거로 활용 |
| R3 | 관찰자 3명의 tally 기준 편차 | 중간 | 항목 단순화 + 사전 5분 정렬. [B] 시간 측정은 기준이 명확해 편차가 작음 |
| R4 | 요약카드를 직원이 안 봄 | 높음 | W1에 직원을 설계에 참여시킴. 직원이 원하는 형태로 만들면 사용한다 |
| R5 | 시각 계산 오류 | **치명적** | 안전 방향 내림 불변조건 + 단위 테스트 필수. 경계값(자정 넘김, 오전 예약) 집중 검증. **+ kill switch 상시 보유 (아래)** |
| R6 | 환자가 서비스 안내를 병원 공식 안내로 오인 | 높음 | §11 필수 표기 문구를 모든 화면 하단에 고정 |
| R7 | Primary KPI가 유의미하게 안 움직임 | 중간 | 정직하게 보고한다. "유의한 변화 없음"을 쓰는 것이 발표 신뢰도를 오히려 높인다 |

### R5 대응 — kill switch (v1.1 추가)

R5는 "즉시 배포 중단"이 대응인데, **v1에는 중단할 수단이 없다.** 스티커는 이미 벽에 붙어 있고, Vercel에서 페이지를 내리면 QR이 404가 되어 환자에게는 "고장난 QR"로만 보인다.

환경변수 하나로 전 화면을 대체하는 스위치를 **배포 전에** 넣어 둔다.

```
NEXT_PUBLIC_SERVICE_STATUS=maintenance
  → 모든 라우트가 계산 결과 대신 아래 화면만 표시

    지금은 안내를 제공하지 않습니다.
    검사 준비사항은 접수처 또는 1599-3114로 문의해 주세요.
```

조건: 재배포 없이 즉시 반영될 것, 계산 로직보다 **먼저** 평가될 것. 구현 비용 10분, 사고 시 유일한 방어선이다.

---

## 19. 미해결 사항

| 항목 | 확정 기한 | 막히는 작업 |
|---|---|---|
| 문답 5개 최종 확정 (W0 tally 9번 분석 후) | W2 착수 전 | T8 |
| 요약카드 항목 순서 + `triage` level 값 확정 (W1 직원 피드백) | W2 착수 전 | T8 |
| **서비스 정식 명칭** | **W3 착수 전** | T13 (QR·스티커) |
| **도메인** | **W3 착수 전** | T13 — QR 이미지가 도메인을 담으므로, 나중에 바꾸면 인쇄물을 전부 다시 만든다 |
| 접수처 스티커 문구 및 부착 위치 | W3 | T13 |
| 안내지 출력 템플릿의 소유 부서 확인 | W5~W7 | C2 (QR 삽입 요청) |
| `medication_categories` 항목 적정성 (실무에서 실제로 묻는 약) | W1 직원 시연 | T2 룰셋 |

명칭·도메인은 v1.0에서 기한 없는 목록에 있었으나, **QR 이미지 생성(T13)의 선행 조건**이므로 W3 착수 전으로 못 박는다.

---

## 부록 A. 제품 철학

> **AI가 환자의 질문에 답하는 것이 아니라, 병원에 도착하기 전에 환자의 상태를 정리해 둔다.**

시장의 대부분의 의료 AI는 **질문에 답하는 AI**다. 이 제품은 **질문이 필요 없게 만드는 도구**를 지향한다. 그래야 핵의학과 직원이 단순 반복설명이 아니라 실제 판단이 필요한 환자에게 집중할 수 있다.

## 부록 B. 참고 — 안내지 원문 근거

본 PRD의 룰셋은 「핵의학 PET 검사 안내(지하 1층)」 안내지에 근거한다.

**최종 대조: 2026-08-10** (실물 확인 완료)

| 항목 | 안내지 원문 기준 |
|---|---|
| 도착 | 예약시간 **20분 전** |
| 검사 소요시간 | 약 **1시간 20분** |
| 금식 | 검사 전 **6시간** |
| 음용 | **생수만 가능.** 단, 같은 날 다른 검사가 있는지 확인하고 마실 것 |
| 당뇨약·인슐린 | 예약시간 **4시간 전**까지 사용 가능 |
| 혈당 | **200 이하** 가능 |
| 체중 | **140kg 이하** 가능 |
| 운동 | 검사 **전일** 과격한 운동(등산·골프·헬스 등) 금지 |
| 폐쇄공포증 | 별도 예약 필요 |
| 임신 가능성·수유 중 | 사전 고지 필요 |
| 장소 | **암병원 / 본관** 지하 1층 핵의학과 (환자마다 다름) |
| 연락처 | 1599-3114 |

**v1.0의 이 부록에는 대조되지 않은 값이 4개 있었다.** 소요시간 1시간 30분, 체중 130kg, 혈당 200 이상, 장소 암병원 고정, 그리고 생수 단서 누락. 개발 착수 전 실물 대조에서 발견해 정정했다(§9.1).

**룰셋 변경 시 반드시 최신 안내지 원문과 대조한다.** 병원이 기준을 바꾸면 이 서비스는 즉시 틀린 정보를 배포하게 된다.

대조 결과는 이 표에만 적지 말고 **`lib/rules/rules.test.ts` 에 함께 반영한다.** 문서만 고치면 코드는 옛 값으로 남는다.

---

## 부록 C. v1.1 개정 내역

개발 착수 전 문서 정합성 검토(2026-08-10)에서 발견된 모순·구멍을 정리한 결과다.

### 스펙 결정이 바뀐 항목

| # | 항목 | v1.0 | v1.1 | 사유 |
|---|---|---|---|---|
| 1 | 금식 시각 표기 | F1 `21:00` / 룰셋 `"전날 자정"` / §9.2 `"전날 저녁 식사 이후"` — **세 값이 불일치** | **계산값 그대로**(`02:00`) + `fasting.note` 보조 1줄 | 21:00은 어떤 규칙에서도 도출되지 않는 값이었고, "전날 자정"은 32시간 금식으로 읽힐 수 있었다 |
| 2 | 새벽 시각 보정 | `early_morning_shift` 규칙 존재 | **제거** | 경계(06:00 예약)에서 24시간 점프 발생. 안내지에 없는 값을 서비스가 생성 |
| 3 | Primary KPI | 전체 평균 문답 시간 20% 단축 | **요약카드 제시 환자**의 문답 시간 50% 단축 | 도달률 18%에서 전체 평균 20% 단축은 **수학적으로 불가능**했다 (§4.4) |
| 4 | 문답 2·3번 입력 | 자유 입력 (약 이름 등) | **선택지 방식** | 자유 입력 원문이 URL에 실려 메신저 서버를 경유. "개인정보 미수집"이 흐려짐 |
| 5 | 요약카드 개발 시점 | W2 | **W1** | §8 F2가 "W1 직원 피드백으로 확정"이라 했으나 W1에 카드가 없었다. R4 대응 공백 |
| 6 | 카카오톡 공유 | 카카오 SDK 병기 | **Web Share API만** | SDK는 개발자 등록 + 도메인 화이트리스트 필요. 3주 일정 초과 |

### 스펙 구멍을 메운 항목

| # | 항목 | 내용 |
|---|---|---|
| 7 | `triage` 블록 신설 (§9.4) | 배지 판정 조건이 룰셋에 없어 하드코딩이 불가피했다. 카드 예시의 🟡 tell이 어떤 규칙에서 나온 것인지 정의되어 있지 않았다 |
| 8 | `arrival.round` 명시 | 도착 시각 내림 단위 미정의. 시 단위로 내리면 10:50 예약 → 10:00(50분 전) |
| 9 | `restrictions.from` + `display` | 룰셋은 `-24h`인데 F1 출력은 "종일"이었다 |
| 10 | 타임존 KST 고정 (§9.2) | 기기 로컬 타임존을 따르면 해외 체류 보호자가 만든 타임라인이 전부 어긋난다 |
| 11 | kill switch (§18) | R5 대응이 "즉시 배포 중단"인데 중단할 수단이 없었다 |
| 12 | tally 시트 [B] "카드" 칸 (§16) | 이 칸이 없으면 새 Primary KPI를 계산할 수 없다 |
| 13 | tally [B] 기록 주체 명확화 (§16) | "하루 5명"이 관찰자당인지 전체인지 모호해 표본 수가 3배까지 달라졌다 |
| 14 | `flags`에 `glucose` 반영 (§8 F2) | 룰셋에는 있는데 문답 체크 항목에서 누락 |
| 15 | Disclaimer "모든 화면" (§11) | "첫 화면 하단"으로 되어 있었으나, 공유 링크 진입 시 S2·S4가 첫 화면이 된다 |
| 16 | TTS fallback 정의 (§8 F3) | Android는 한국어 음성이 없을 수 있다. 눌러도 소리가 안 나는 버튼은 "고장난 서비스"로 읽힌다 |
| 17 | 웹폰트 금지 (§13) | 한글 웹폰트로는 3G 1.5초 기준을 맞출 수 없다 |
| 18 | 명칭·도메인 기한 설정 (§19) | QR 이미지 생성의 선행 조건인데 기한이 없었다 |

워크플로우 문서의 대응 변경은 `WORKFLOW.md` 부록 C에 있다.

---

## 부록 D. v1.2 개정 내역

T2(룰셋 작성) 착수 시 **안내지 실물을 대조**하면서 나온 변경이다. 문서만 보고는 알 수 없었던 것들이다.

| # | 항목 | 변경 | 사유 |
|---|---|---|---|
| 1 | 룰셋 값 5건 | 소요시간 80분 / 체중 140kg / 혈당 200 초과 / 생수 단서 추가 / 장소 선택지화 | 안내지 원본과 어긋나 있었다. §9.1 |
| 2 | 검사 장소 | 단일 `location` → `locations` 선택지, **S1에서 질문** | 건물이 암병원·본관 두 곳이고 환자마다 다르다. 추측하면 다른 건물로 걸어간다 |
| 3 | 당뇨 항목 | "당뇨 응답이 예이면 추가" → **조건부 문구로 항상 표시** | 당뇨 문항은 S3인데 이 항목은 S2다. **화면 흐름상 성립하지 않는 스펙이었다.** 입력을 늘리지 않고 해결 |
| 4 | 체크 항목 | 4개 → **3개**. 화면 구성은 **1화면 1탭** | 1문 1화면으로 하면 40초 기준을 못 맞춘다. 대부분 전부 해당 없음 |
| 4-1 | **혈당** | `flags` 체크 → **`arrival.note` 사실 표기** | **당일 병원이 전 환자에게 측정하는 값.** 자가 응답으로 🟢 를 띄우면 거짓 안심이 된다. 문답 4번 당뇨와도 중복이었다 |
| 5 | 타임라인 줄 수 제한 | "3줄" → 보조 2줄, 금식만 3줄 | 생수 단서를 넣으면 금식 항목이 4줄이 된다. 단서를 빼면 틀린 안내가 되므로 제한을 완화 |
| 6 | 부록 B | 대조 일자·항목 전면 갱신 | 대조되지 않은 값이 4개 있었다 |

**이번 개정의 교훈:** v1.1 검토는 두 문서를 서로 대조해 모순 18건을 찾았지만, **문서 밖의 사실과는 대조하지 못했다.** 안내지 실물을 한 번 보는 것으로 5건이 더 나왔고, 실무 흐름을 아는 사람의 한마디("혈당은 당일 접수 때 잰다")로 설계 오류가 하나 더 나왔다.

값의 근거가 문서 밖에 있으면 문서 검토로는 잡히지 않는다.

### 새로 세운 원칙 — 무엇을 묻고 무엇을 묻지 않는가

혈당 건에서 나온 분류다. `flags`에 항목을 추가할 때 이 표로 판단한다.

| | 예 | 처리 |
|---|---|---|
| 환자가 **스스로 아는 것** | 체중, 폐쇄공포증, 임신 가능성, 먹은 것, 먹은 약 | **묻는다** → `flags` / 문답 |
| **병원이 당일 측정·판단하는 것** | 혈당, 영상 판독, 주사 부위 | **묻지 않는다** → 사실로 알려만 준다 (`note`) |

두 번째를 첫 번째로 착각하면 **환자의 자가 응답이 병원의 판정인 것처럼 보이는 화면**이 만들어진다. 🟢 배지가 그 순간 거짓말이 된다.
