# kill switch — PRD §18 R5
#
# maintenance 로 두면 모든 화면이 안내 문구로 대체된다.
# 시각 계산 오류가 실환자에게 1건이라도 발생하면 즉시 켠다.
#
# 비워 두거나 다른 값이면 정상 동작한다.
#
# ⚠ Vercel에서는 환경변수를 바꾼 뒤 재배포해야 반영된다 (약 1분).
#    대시보드에서 Redeploy 버튼으로 트리거한다.
NEXT_PUBLIC_SERVICE_STATUS=
